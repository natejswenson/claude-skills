name: resume

# Per-skill CI + release for the resume skill. See devlog.yml for the PR/push
# trigger + change-detection rationale (deadlock avoidance).
on:
  push:
    branches: [main]
    paths: ["skills/resume/**", "tools/score_skill.py", "tools/lint_plugin.py", ".github/workflows/resume.yml"]
  pull_request:
    branches: [dev, main]
  workflow_dispatch:

jobs:
  ci:
    name: ci / resume # distinct check-run name so it can be a required status check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read # required by dorny/paths-filter on pull_request events
    steps:
      - uses: actions/checkout@v7
      - name: Detect relevant changes
        id: changes
        uses: dorny/paths-filter@v4
        with:
          filters: |
            resume:
              - 'skills/resume/**'
              - 'tools/score_skill.py'
              - 'tools/lint_plugin.py'
              - '.github/workflows/resume.yml'
      # Tier 1: shared SKILL.md structural lint
      - if: steps.changes.outputs.resume == 'true'
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - if: steps.changes.outputs.resume == 'true'
        run: python tools/score_skill.py skills/resume/skills/resume --min 100
      # Tier 1.5: plugin.json <-> SKILL.md/package.json name+version consistency
      - if: steps.changes.outputs.resume == 'true'
        run: python tools/lint_plugin.py skills/resume
      # Tier 2: resume offline unit suite (Node). The tailoring-quality eval
      # harness (npm run eval) is NOT run here — it shells a real claude -p
      # call, costs real money, takes 10-90 minutes, and requires human
      # sign-off on its results (see docs/plans/2026-07-08-resume-eval-harness-design.md).
      # It's a manual/on-demand gate, not a CI check.
      - if: steps.changes.outputs.resume == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: skills/resume/skills/resume/package-lock.json
      - if: steps.changes.outputs.resume == 'true'
        run: npm ci
        working-directory: skills/resume/skills/resume
      - if: steps.changes.outputs.resume == 'true'
        run: npm test
        working-directory: skills/resume/skills/resume

  release:
    needs: ci
    # Real push to main (when re-coupled via a PAT) OR a deliberate manual dispatch on main.
    # The bot GITHUB_TOKEN auto-merge doesn't trigger push events, so workflow_dispatch is the on-demand release path.
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    uses: ./.github/workflows/_release.yml
    with:
      skill: resume
    permissions:
      contents: write
