[pytest]
testpaths = tests
# `live` tests hit the real Data USA API. Excluded by default so CI needs no
# network; run them with `pytest -m live` before a release.
addopts = -m "not live"
markers =
    live: hits the live Data USA API (deselected by default)
