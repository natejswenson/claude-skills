{
  "$comment": "The known-bad side. Every defect here is one that passes `score_skill.py` and every other lint in this repo, and still produces a skill that never triggers, cannot be reasoned about, and degrades invisibly. If check-spec ever accepts this file, the gate has been weakened rather than the input fixed — which is the exact way a one-sided baseline rots.",
  "name": "tally",
  "summary": "Counts things.",
  "description": "Counts stuff in a repo.",
  "oneRule": "Be good.",
  "stack": "node",
  "commands": [
    { "name": "count", "does": "counts" }
  ],
  "split": {
    "deterministic": [
      { "step": "count the things" }
    ],
    "nondeterministic": []
  },
  "references": [],
  "evalPlan": [
    {
      "id": "it-works",
      "kind": "golden"
    }
  ]
}
