# sources

where every fact comes from: known_marketplaces.json, each marketplace.json, each plugin.json, and claude plugin list --json

> Scaffolded by smith. This file is part of pluginsync's contract — SKILL.md
> points at it, so it must say something real before the skill ships.
