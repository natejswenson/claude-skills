▶ brief — the plan is delivered — briefing red-team round 1

| Review of   | Round  | Model | Agent           |
|-------------|--------|-------|-----------------|
| investigate | 1 of 3 | opus  | general-purpose |

It must write: <RUN>/reviews/investigate-r1.findings.json

Dispatch ONE subagent, model `opus`, with exactly this prompt:

  Read <RUN>/briefs/review-investigate-r1.md and follow it exactly. It is your complete brief.


next: dispatch (brief)
  The prompt(s) to dispatch are printed above.

wait: sh -c 'end=$(( $(date +%s) + 1800 )); until [ '\''<RUN>/reviews/investigate-r1.findings.json'\'' -nt '\''<RUN>/briefs/review-investigate-r1.md'\'' ]; do [ $(date +%s) -ge $end ] && exit 124; sleep 5; done; a=$(wc -c < '\''<RUN>/reviews/investigate-r1.findings.json'\'' 2>/dev/null); sleep 20; b=$(wc -c < '\''<RUN>/reviews/investigate-r1.findings.json'\'' 2>/dev/null); while [ "$a" != "$b" ]; do a=$b; sleep 20; b=$(wc -c < '\''<RUN>/reviews/investigate-r1.findings.json'\'' 2>/dev/null); done'
then: node '<SKILL>/scripts/issueflow.js' next --run-dir '<RUN>'
