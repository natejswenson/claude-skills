{
  "f-9674e7de": { "status": "fixed", "note": "test_the_latency_gate_is_still_armed now parses pyproject.toml's addopts via tomllib and asserts the flag is in the parsed addopts string, not a raw file-text substring match, so pyproject.toml:55's comment can no longer keep the assertion green after the real flag is removed." },
  "f-eea114d4": { "status": "fixed", "note": "test_get_training_plan_progress_rounds_without_a_call_per_node now freezes tools.date to _TODAY+58d via a new _freeze_progress_clock helper and asserts an exact count (83), so the assertion no longer drifts with wall-clock date." },
  "f-0583160f": { "status": "fixed", "note": "test_get_training_plan_progress_does_not_double_classify now asserts counts[\"n\"] == 87 (measured invariant across every date probed) instead of <= 90, so an early-return handler producing 0 calls now fails it." },
  "f-7098c483": { "status": "fixed", "note": "test_get_training_plan_progress_rounds_without_a_call_per_node now asserts counts[\"n\"] == 83 (with the clock frozen per f-eea114d4) instead of <= 100, so an early-return handler producing 1 call now fails it." },
  "f-7115b02e": { "status": "fixed", "note": "added a mixed-day case to _WORKOUT_ACTUALS_EDGE_CASES ([_RIDE, _PACED_RUN, _PACED_WALK, untyped-non-foot]) plus a floor on the edge-case list length; verified against a real mutant (continue -> break in the non-foot branch) that the 548-day corpus and old 6 edge cases could not catch but the new mixed-day case does." },
  "f-73a7ebc6": { "status": "fixed", "note": "same change as f-7115b02e — the suggested mixed-day case and the len(_WORKOUT_ACTUALS_EDGE_CASES) >= 7 floor were both added." },
  "f-63b3df6d": { "status": "fixed", "note": "extended the work-COUNT axis's docstring paragraph to state it runs in the ordinary pytest job and is skipped under --benchmark-only, the inverse of the latency axis's skip rule." },
  "f-7ede0eb0": { "status": "fixed", "note": "added direct isinstance assertions (a bare tuple and a list-of-tuples) after the existing _dumped byte-compare loop; verified against a real mutant that correctly rounds tuple elements but preserves tuple type, which the byte-compare passed over and the new assertions catch." },
  "_summary": "Pushed commit c577f1f to feature/issue-232-perf-plan-progress-regression (origin), addressing all 8 open findings in tests/test_perf_benchmarks.py with no src/ changes."
}
