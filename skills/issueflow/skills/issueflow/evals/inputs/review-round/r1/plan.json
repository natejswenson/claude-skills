{
  "verifiers": 8,
  "candidateIds": [
    "c-1-1",
    "c-1-2",
    "c-1-3",
    "c-1-4",
    "c-1-5",
    "c-1-6",
    "c-1-7",
    "c-1-8",
    "c-1-9",
    "c-1-10",
    "c-2-1",
    "c-2-2",
    "c-2-3",
    "c-2-4",
    "c-2-5",
    "c-2-6",
    "c-2-7",
    "c-2-8",
    "c-3-1",
    "c-3-2",
    "c-3-3",
    "c-3-4",
    "c-3-5",
    "c-3-6",
    "c-3-7",
    "c-3-8",
    "c-3-9",
    "c-3-10",
    "c-4-1",
    "c-4-2",
    "c-4-3",
    "c-4-4",
    "c-4-5",
    "c-4-6",
    "c-4-7",
    "c-4-8",
    "c-4-9",
    "c-4-10"
  ],
  "priorIds": [],
  "candidates": [
    {
      "id": "c-1-1",
      "file": "tests/test_perf_benchmarks.py",
      "line": 515,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "The `--benchmark-skip` half of `test_the_latency_gate_is_still_armed` cannot fail: `pyproject.toml` line 55 is a comment that begins `# --benchmark-skip:`, so the substring survives deletion of the flag from `addopts`.",
      "short_summary": "--benchmark-skip grep assertion can never fail",
      "failure_scenario": "Verified: `sed '59s/ --benchmark-skip//' pyproject.toml | grep -c -- '--benchmark-skip'` returns 1 — the comment at pyproject.toml:55 (\"# --benchmark-skip: the perf-benchmark suite (tests/test_perf_benchmarks.py) / only runs when explicitly requested via --benchmark-only...\") keeps the string alive. Someone removes `--benchmark-skip` from `addopts` (line 59); every ordinary `uv run pytest` then runs the six timed latency benchmarks, and the test whose docstring says it guards \"the `--benchmark-skip` default that keeps these benchmarks off an ordinary `pytest` run\" stays green. Directly against CLAUDE.md:113-125: \"The bar is 'would this test FAIL if the code under test were broken?' — if not, it isn't a test.\" (The sibling assertion on line 513 is sound: `--benchmark-compare-fail=min:15%` appears exactly once in ci.yml, in the run command at :72, with no comment mentioning it.)",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    import tomllib\n    addopts = tomllib.loads((root / \"pyproject.toml\").read_text())[\"tool\"][\"pytest\"][\"ini_options\"][\"addopts\"]\n    assert \"--benchmark-skip\" in addopts",
      "prior": false
    },
    {
      "id": "c-1-2",
      "file": "tests/test_perf_benchmarks.py",
      "line": 373,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "The equivalence oracle's anti-vacuity floor (`assert len(by_date) > 500`) presents a 548-day corpus that is measurably one input shape repeated 548 times, and neither it nor the six edge cases contains a multi-activity day — the exact case the fold changed.",
      "short_summary": "548-day oracle corpus is one shape, 548 times",
      "failure_scenario": "Measured against the real fixture (scripts/perf_fixture.py:196-206 seeds one row per date, always `activity_type='running'`, always with no `avg_pace_sec_per_km`): 548 days, max 1 activity/day, exactly 1 distinct classification shape `(('running', paceless, dist>0),)`, and every single day returns `['running']`. The `walking` branch, the `other` branch and every multi-activity list are never differentially compared. The fold's actual control-flow change is that `classes.add('other')` now runs inside the loop that accumulates `foot`/`dur` and hits `continue` before the distance work — so a day holding one on-foot activity and one ride is the case that would catch an ordering or accumulation mistake, and no case in `_WORKOUT_ACTUALS_EDGE_CASES` (all six are single-activity) or in the 548-day loop is one. Combined with `test_workout_actuals_evaluates_ran_once_per_on_foot_activity` (:269), which passes a mixed 3-activity day but asserts only the `_ran` count and never the returned classes, the mixed-day fold is differentially untested here; only `tests/test_plans.py:test_workout_actuals_returns_foot_run_and_walk_in_one_pass` covers it, and it is not an oracle against the pre-fold implementation. Same CLAUDE.md rule as above (\"never coverage theater\", :113-125) plus its \"Cover the edge cases\" clause.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "Add a mixed day to `_WORKOUT_ACTUALS_EDGE_CASES`, e.g. `[_PACED_RUN, _PACED_WALK, _RIDE, {\"activity_type\": None, \"distance_meters\": 100.0}]`, and assert a floor on `len(_WORKOUT_ACTUALS_EDGE_CASES)` the way `_ROUND_FLOATS_CASES` does at :506.",
      "prior": false
    },
    {
      "id": "c-1-3",
      "file": "src/local_fitness/agent/tools.py",
      "line": 169,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "The new docstring parenthetical claims the `bool` guard \"becomes load-bearing\" under the fast path and forbids any test showing otherwise; measured, the guard is still completely inert after this change.",
      "short_summary": "Docstring says bool guard is now load-bearing; it isn't",
      "failure_scenario": "Deleted `if isinstance(obj, bool): return obj` (tools.py:187-188) from the post-change implementation and differentially fuzzed it against the pre-inline reference over 40,000 randomly generated nested payloads (dicts/lists/dict-subclass/list-subclass containers; leaves covering float, float/int subclasses, bool, int, str, None, NaN, +/-inf, -0.0, tuple, bytes, Decimal, date, frozenset, OrderedDict; high-precision and non-string keys), comparing structurally with type preservation, not via json.dumps: 0 divergences — identical to the 0 divergences the unmodified new implementation produces. The reason is that the guard was never reachable in a way that mattered and still isn't: both container fast paths copy a `bool` child inline via `t is bool` (lines 199, 211) and never recurse for it, and the fallback `isinstance` ladder (lines 220-225) still has no `int` branch, so a bool arriving at the top falls all the way through to `return obj` unchanged. There is no `int` fast path that rounds — line 199/211 only copy. Cost: the comment states a false invariant about the code three lines below it, and instructs the next author (\"no test may claim removing it is observable\") on the strength of that false reason, so the guard's real status — dead-but-harmless defensive code — is now recorded backwards in the one place a reader checks.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-1-4",
      "file": "tests/test_perf_benchmarks.py",
      "line": 16,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "The new third axis never runs in the invocation this file's own docstring says is the only one that runs it — `--benchmark-only` skips every non-benchmark test, so all six new tests are skipped in CI's perf-gate step.",
      "short_summary": "Third axis is skipped by the only run the docstring names",
      "failure_scenario": "Ran it: `pytest tests/test_perf_benchmarks.py --benchmark-only` reports `6 passed, 16 skipped` with `SKIPPED [16] Skipping non-benchmark (--benchmark-only active).` — pytest-benchmark's `pytest_collection_modifyitems` (plugin.py:336-347) marks every item without the `benchmark` fixture skipped under `--benchmark-only`. `.github/workflows/ci.yml:70` is exactly that invocation, so the entire work-COUNT axis and both equivalence oracles are skipped in the \"Perf-benchmark regression gate\" step and only execute in the \"Run tests with coverage gate\" step. Lines 4-7 of this same docstring — unchanged by the diff — say \"a plain `uv run pytest` never runs the ... benchmarks here — only an explicit `--benchmark-only` invocation does\", and the diff appends \"Three axes are asserted\" under it. A maintainer debugging a red perf gate reads that header, expects the work counts to be part of what the gate reported, and will not find them; a maintainer adding a fourth work-count assertion will reasonably put it behind the `benchmark` fixture and silently disable it in the coverage job instead. The axis does run in CI (in the other step), so this is a documentation/placement defect, not dead code.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "Add one sentence to the axis list saying the work-COUNT axis runs in the ordinary `pytest` job and is skipped under `--benchmark-only`, the inverse of the latency axis.",
      "prior": false
    },
    {
      "id": "c-1-5",
      "file": "CLAUDE.md",
      "line": 161,
      "side": "RIGHT",
      "category": "conventions",
      "summary": "The PR adds a third assertion axis to the perf suite and edits the same CLAUDE.md bullet, but leaves that bullet's description of what the suite asserts at \"latency AND `db.connect()`-open-count\".",
      "short_summary": "CLAUDE.md perf bullet still says two axes, not three",
      "failure_scenario": "CLAUDE.md:234-239 states the rule verbatim: \"**Keep CLAUDE.md current — in the same commit/PR.** Any change that alters the workflow, architecture, deploy/branch model, security contract, or an env var updates the relevant CLAUDE.md section as part of that same commit, not as a follow-up. CLAUDE.md is the source of truth future-you reads first; a diff that changes behavior but leaves CLAUDE.md stale is incomplete.\" The diff edits lines 199-221 of this very bullet (adding drift-tell part 3) and updates the test module docstring from \"Two axes are asserted\" to \"Three axes are asserted\", yet CLAUDE.md:160-161 still reads \"separate axis from the coverage gate: latency AND `db.connect()`-open-count for the brief/plan hot paths\". Line 176 of the same bullet — \"Skipped on every ordinary `pytest` run (`--benchmark-skip` in `pyproject.toml`'s `addopts`)\" — is now false for this file in the direction that matters: 16 of its 22 tests, including all 6 new ones, run on every ordinary `pytest` and are skipped by the perf-gate step (verified above). The next person reading CLAUDE.md first, as the rule tells them to, will not know the work-count axis exists or which job runs it.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-1-6",
      "file": "tests/test_perf_benchmarks.py",
      "line": 427,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "`_dumped`'s `json.dumps` comparison is type-blind, so the oracle cannot assert the tuple-to-list conversion the cases at :412 and :413 exist to cover, nor any container/scalar subclass identity.",
      "short_summary": "json.dumps oracle can't see tuple vs list",
      "failure_scenario": "`json.dumps((1.23, 2.35)) == json.dumps([1.23, 2.35]) == '[1.23, 2.35]'`, and `default=str` further collapses anything unserializable to its `str()`. The approved plan's Proof 5 names \"a `tuple` (must become a `list`)\" as a required case and `_ROUND_FLOATS_CASES` duly carries `(1.23456789, 2.3456789)` and `[(1.23456789,), (2.3456789,)]` — but the comparison function cannot distinguish pass from fail on that property. A later fast-path extension such as `if type(obj) is tuple: return obj` (a tempting next optimization, since tuples currently take the slow ladder) changes `_round_floats`'s return type contract and leaves both oracle tests green. The same blindness covers `_FloatSubclass`/`_IntSubclass`/`_DictSubclass`: `_mishandles_a_dict_subclass` only diverges because its *values* stay unrounded, not because its type differs, so the two-sidedness proof at :463 does not demonstrate type coverage either. (For the record, a type-preserving 40k-case differential fuzz confirms the shipped implementation IS equivalent today — the gap is that the committed oracle would not notice if it stopped being.)",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "Compare with a type-preserving normalizer, or add one direct assertion: `assert isinstance(tools._round_floats((1.0, 2.0)), list)`.",
      "prior": false
    },
    {
      "id": "c-1-7",
      "file": "src/local_fitness/plans.py",
      "line": 1205,
      "side": "RIGHT",
      "category": "efficiency",
      "summary": "The fold removes one redundant pass over the day's activity list but leaves another in the same caller loop: `classify_workout` recomputes `_running_distance`/`_foot_distance`, values `_workout_actuals` returned two lines earlier.",
      "short_summary": "grade_workout re-walks the day list just walked",
      "failure_scenario": "In `build_plan_detail` (plans.py:1254-1257) each iteration calls `_workout_actuals(day, cfg)` — which returns `foot` and `run_m` — and then immediately `grade_workout(w, day, frontier, cfg)`, whose `classify_workout` recomputes `_foot_distance(day_activities)` (plans.py:337) or `_running_distance(day_activities, cfg)` (plans.py:339) or `_running_duration(day_activities, cfg)` (plans.py:351) over the same list, evaluating `_ran` once more per activity. Because `_ran` checks on-foot first (plans.py:165-166, \"ON-FOOT IS CHECKED FIRST\"), `_running_distance(day, cfg)` is exactly the `run_m` already in hand and `_foot_distance(day)` is exactly `foot` — the recomputation is byte-identical work. Measured on the perf fixture at this head: the handler still makes 87 `_ran` calls (and 83 `_round_floats` calls) for one `get_training_plan_progress` request; the fold's own accounting (one `_ran` per on-foot activity) implies roughly a third of that. Cheaper alternative: have `_workout_actuals` also accumulate running duration and pass its five/six-tuple into `grade_workout`/`classify_workout` instead of letting them re-derive it. This predates the diff, but the diff's new docstring sentence on this very line — \"Anything else this day needs is computed in the loop below, not in a pass of its own\" — is contradicted by the next statement in its only caller, so the invariant it re-asserts is still not true at the handler level.",
      "introduced_by_diff": false,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-1-8",
      "file": "src/local_fitness/agent/tools.py",
      "line": 197,
      "side": "RIGHT",
      "category": "efficiency",
      "summary": "The new hot loops do a global lookup for `_TEXT_HIGH_DP`, `_TEXT_DEFAULT_DP`, `_TEXT_HIGH_PRECISION_KEYS` and `round` on every float child, and an attribute lookup for `out_l.append` on every list item, in the function this change exists to make faster.",
      "short_summary": "Hot loop re-loads globals and .append per element",
      "failure_scenario": "Cost, not a crash: the dict branch (tools.py:196-198) evaluates `round`, `_TEXT_HIGH_DP`, `_TEXT_HIGH_PRECISION_KEYS` and `_TEXT_DEFAULT_DP` as LOAD_GLOBALs (module-dict hash lookups) for every float value it rounds; the list branch repeats `round` per item (line 210) and resolves `out_l.append` per item (lines 210-213). On the measured payload that is hundreds of float children per request, in the one function the CHANGELOG says accounts for 49.6% of its own runtime. Cheaper alternative: bind them once — `def _round_floats(obj, key=None, *, _hi=_TEXT_HIGH_DP, _lo=_TEXT_DEFAULT_DP, _keys=_TEXT_HIGH_PRECISION_KEYS, _round=round)` (or plain locals at the top of each branch), and `append = out_l.append` before the list loop. This is a nit on top of a change that already delivers its measured win, not a defect in it.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-1-9",
      "file": "tests/test_perf_benchmarks.py",
      "line": 431,
      "side": "RIGHT",
      "category": "efficiency",
      "summary": "`_capture_progress_payload` re-runs the whole `get_training_plan_progress` handler over the 3-year fixture once per calling test, for a payload that is identical both times.",
      "short_summary": "Progress payload rebuilt per test instead of once",
      "failure_scenario": "Cost: the new block adds four full `get_training_plan_progress.handler({})` invocations to the ordinary `pytest` job — one each in `test_get_training_plan_progress_does_not_double_classify` (:279) and `test_get_training_plan_progress_rounds_without_a_call_per_node` (:287), which genuinely need their own counted run, plus one each in `test_round_floats_output_is_identical_to_the_pre_inline_implementation` (:449) and `test_the_round_floats_oracle_can_actually_fail` (:463), which both want the same unchanging payload object. During capture the handler also runs with `_round_floats` wrapped, so every node pays two Python frames instead of one. Cheaper alternative: make the captured payload a module-scoped fixture (built once alongside `perf_db`) and have both oracle tests consume it, removing one full handler run and one wrapped traversal from every suite run — the suite CLAUDE.md:126 tells you to run before claiming done.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-1-10",
      "file": "tests/test_perf_benchmarks.py",
      "line": 284,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "`assert counts[\"n\"] <= 90` sits three calls above the measured 87, so an unrelated perf-fixture change reddens it with a message about double classification.",
      "short_summary": "_ran bound of 90 has 3 calls of headroom over 87",
      "failure_scenario": "Measured at this head: the handler makes exactly 87 `_ran` calls against the current fixture. `scripts/perf_fixture.py` seeds one activity per date; adding a second activity to even two days, or adding the paced rows CLAUDE.md:167-177 explicitly discusses as a recurring temptation, pushes the count past 90 and fails a test named `test_get_training_plan_progress_does_not_double_classify` with a bare `assert 92 <= 90` — a fixture change reported as a classification regression. The sibling bound at :297 (`<= 100` against a measured 83) has ample room; only the `_ran` bound is tight. The bounds are the approved plan's own numbers (Proof 2/3), so this is a robustness note rather than a deviation.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "Assert against a per-on-foot-activity ratio, or raise the bound to a value that still fails at the pre-fold 123 (e.g. `<= 105`) and put the measured 87 in the message.",
      "prior": false
    },
    {
      "id": "c-2-1",
      "file": "src/local_fitness/agent/tools.py",
      "line": 168,
      "side": "RIGHT",
      "category": "language-pitfalls",
      "summary": "The new docstring claims the `int` fast path makes the top-level `bool` guard load-bearing; it does not, and the diff's own test comment says the opposite.",
      "short_summary": "bool-guard rationale is false and self-contradictory",
      "failure_scenario": "The added parenthetical says \"there is no ``int`` branch there and ``isinstance(True, float)`` is False — but the ``int`` fast path below makes it load-bearing\". It does not: the container fast paths match `t is bool` (tools.py:199, :211) before any recursion, and even with that arm deleted a bool never satisfies `t is int` because `type(True) is bool`, so a bool still falls to the ladder and still returns unchanged. Verified empirically at the head commit: deleting `if isinstance(obj, bool): return obj` (tools.py:187) produces byte-identical output over the real `get_training_plan_progress` payload and every case in `_ROUND_FLOATS_CASES`, plus bools nested in dicts, lists, tuples and dict subclasses. The diff's own test at tests/test_perf_benchmarks.py:470 asserts the contrary of the docstring (\"removing the `bool` guard diverges on nothing\"), so the two halves of one change disagree. Concrete cost: a maintainer who later adds a genuine `int` branch to the ladder will read this docstring as saying the guard already protects that case and will not add the test that would catch `True` serializing as `1`.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    (That guard is inert today — there is no ``int`` branch in the ladder,\n    ``isinstance(True, float)`` is False, and the container fast paths match\n    ``type(v) is bool`` before recursing — but it is the thing that keeps an\n    ``int`` branch from ever swallowing a bool, so it stays.)",
      "prior": false
    },
    {
      "id": "c-2-2",
      "file": "src/local_fitness/plans.py",
      "line": 1256,
      "side": "RIGHT",
      "category": "altitude",
      "summary": "The fold removes only 36 of the handler's 87 `_ran` evaluations; the other 51 come from `grade_workout` re-walking the very same day list three more times in the same loop iteration, which is the identical defect class the diff was sent to fix.",
      "short_summary": "grade_workout still re-walks the same day's activities",
      "failure_scenario": "Measured at the head commit against the perf fixture (`build_perf_fixture_db(today=2026-07-09)`), one `get_training_plan_progress` request: `_ran` is evaluated 87 times, of which only 36 come from `_workout_actuals` (one per activity — the fold works) and 51 come from the generator expressions in `_running_distance` (plans.py:189), `_walking_distance` and `_running_duration` (plans.py:213), reached through `grade_workout` on line 1259 — over the *same* `day` list that line 1256 just walked, in the same loop iteration. `weekly_mileage` (plans.py:1279, via the `dist_fn` at plans.py:532) walks them a fourth time. `_workout_actuals` already returns `foot`, `run` and `walk` and already accumulates `dur`, so `grade_workout` is recomputing values its caller is holding. The simpler, more general change is to hand `grade_workout` the actuals tuple `_workout_actuals` just produced (or return `dur` alongside it) instead of letting it re-derive them — that removes 51 of the 87 remaining evaluations and three list walks per graded day, versus the 36 this diff removes. As shipped, the new gate at tests/test_perf_benchmarks.py:284 pins `<= 90`, which cements the remaining duplication as the accepted floor, and `_workout_actuals`'s docstring keeps claiming \"ONE pass ... `_ran` is evaluated at most once per activity\" while the request as a whole evaluates it 2.4x per activity.",
      "introduced_by_diff": false,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-2-3",
      "file": "src/local_fitness/plans.py",
      "line": 1231,
      "side": "RIGHT",
      "category": "altitude",
      "summary": "Inside the loop this diff rewrote, `_ran(a, cfg)` re-runs the `_is_on_foot` substring scan that the line above already computed, so the loop does more label-matching work than the `_ran` count it optimises.",
      "short_summary": "_ran redoes the _is_on_foot check the loop just did",
      "failure_scenario": "plans.py:1222 computes `_is_on_foot(at)` and `continue`s when it is False; line 1231 then calls `_ran(a, cfg)`, whose first statement (plans.py:167-169) is `if not _is_on_foot(at): return False` — the identical call on the identical value. Measured at the head commit on the perf fixture, one `get_training_plan_progress` request over 36 activities: `_is_on_foot` 139 calls and `_is_running` 226 calls, against 87 `_ran` calls. `_is_running`/`_is_walking` each lowercase the label and run an `any(...)` substring scan over a frozenset, so this is strictly more work per activity than the `_ran` dispatch the diff counts — and the new assertion counts `_ran` only, so the duplication is invisible to the gate the change adds. The deeper fix in the lines the diff already touches: split `_ran` into an on-foot-known variant (or hoist `at.lower()` once per activity) so the loop's known-on-foot branch does not pay for the check a second time. Cleanup, not a crash.",
      "introduced_by_diff": false,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-2-4",
      "file": "tests/test_perf_benchmarks.py",
      "line": 284,
      "side": "RIGHT",
      "category": "altitude",
      "summary": "The `_ran` work-count assertion is an upper bound with no floor, so it passes over zero work if the fixture ever stops producing an active plan.",
      "short_summary": "_ran count assertion has no anti-vacuity floor",
      "failure_scenario": "`get_training_plan_progress` short-circuits at tools.py:3764 — `if active is None: return _text({\"active\": False})` — before any grading runs. If `plans.get_active_plan` ever stops matching the fixture's plan (a `status` value renamed, a schema migration, a change to `scripts/perf_fixture.py`'s `_seed_plan`, or the plan's date window moving out of range), `_ran` is called 0 times, `assert counts[\"n\"] <= 90` passes, and the assertion the whole PR is built on measures nothing while still reporting green — the exact failure mode the change's own CHANGELOG entry says the latency axis suffered from for five weeks. The two sibling tests added in the same block DO carry floors (`assert len(by_date) > 500` at line 373; `assert payload.get(\"workouts\")` at line 455), so the omission is inconsistent within this one change. CLAUDE.md:118-120: \"The bar is 'would this test FAIL if the code under test were broken?' — if not, it isn't a test.\" Measured actual at head: 87.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    assert 80 <= counts[\"n\"] <= 90, counts[\"n\"]",
      "prior": false
    },
    {
      "id": "c-2-5",
      "file": "tests/test_perf_benchmarks.py",
      "line": 297,
      "side": "RIGHT",
      "category": "altitude",
      "summary": "The `_round_floats` work-count assertion is likewise upper-bound-only, and an empty/error payload makes it pass at a single call.",
      "short_summary": "_round_floats count assertion has no floor either",
      "failure_scenario": "With no active plan the handler returns `_text({\"active\": False})`, which is one `_round_floats` call on a two-key dict; `assert counts[\"n\"] <= 100` passes. The same is true of any payload shrink — the tool's own docstring says the response is windowed by default, so a windowing change that dropped the `workouts` list would collapse the count to a handful and the test would still be green while the assertion it documents (\"470 before, 83 after\") no longer describes anything. Unlike its neighbour at line 455 this test never asserts the payload is real. Measured actual at head: 83.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    assert 70 <= counts[\"n\"] <= 100, counts[\"n\"]",
      "prior": false
    },
    {
      "id": "c-2-6",
      "file": "src/local_fitness/plans.py",
      "line": 227,
      "side": "LEFT",
      "category": "removed-behaviour",
      "summary": "Deleting `_normalize_activity_types` removes the only way to ask \"what activity classes were on this day\" without also running the distance/pace accumulation, which is what invites the next caller to re-add a second pass.",
      "short_summary": "no standalone classifier left after the fold",
      "failure_scenario": "Before the diff, a consumer needing only the day's classes called `_normalize_activity_types(day, cfg)`. After the fold the only entry point is `_workout_actuals(day, cfg)[4]`, which also accumulates foot/run/walk distance and pace and allocates a set — so a future caller in the plan view, `assemble_status`, or the report-card path that wants classes alone must either discard four computed values or write its own loop. Writing its own loop over the same list is precisely how PR #162 introduced the regression this PR is fixing (the docstring at plans.py:1200-1206 says so), and the retargeted tests at tests/test_plans.py:582-609 now go through the five-tuple, so nothing in the suite would flag a re-added standalone classifier as a duplicate pass. The safer shape is to keep a thin `_activity_classes(day, cfg)` that the fold calls once per activity via the same loop, or to note in the docstring that classes are only ever obtainable from the five-tuple so the next caller does not re-derive them.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-2-7",
      "file": "src/local_fitness/agent/tools.py",
      "line": 197,
      "side": "RIGHT",
      "category": "altitude",
      "summary": "The precision rule is now written out four times inside one function and the type ladder twice, with only a test keeping the fast path and the fallback in agreement.",
      "short_summary": "dp rule duplicated 4x, type ladder duplicated 2x",
      "failure_scenario": "`_TEXT_HIGH_DP if <key> in _TEXT_HIGH_PRECISION_KEYS else _TEXT_DEFAULT_DP` appears at tools.py:190, :197-198 and :205, and the `float`/`str`/`int`/`bool`/`None` classification appears at :187-191, :196-200 and :209-212 — three near-copies whose only difference is that the dict arm keys off the child key `k` and the list arm off the enclosing `key`. Cost: any change to the precision rule (a suffix match, a per-tool override, a third precision tier) has to land in four places and get the `k`-vs-`key` distinction right in each; the docstring itself concedes \"the fast path must stay semantically identical to the fallback\" and delegates that guarantee to a test rather than to the code's shape. A single local `def _dp(k): ...` (or hoisting the scalar classification into one helper the branches share) keeps one statement of the rule at a fraction of the per-leaf recursion cost the change is buying back. Cleanup, not a defect — a 40k-case differential fuzz of the new implementation against the vendored reference (dict/list subclasses, tuples, non-str dict keys, Decimal, date, bytes, IntEnum, NaN/inf) found zero divergences at this head.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-2-8",
      "file": "CLAUDE.md",
      "line": 161,
      "side": "RIGHT",
      "category": "removed-behaviour",
      "summary": "The change adds a third assertion axis to the perf suite and updates the test module's docstring, but leaves CLAUDE.md's own description of that gate saying it has two.",
      "short_summary": "CLAUDE.md still says the perf gate has two axes",
      "failure_scenario": "The diff rewrites tests/test_perf_benchmarks.py's docstring from \"Two axes are asserted\" to \"Three axes are asserted\" and adds part (3) of the drift tell at CLAUDE.md:199-221, but CLAUDE.md:160-161 still reads \"latency AND `db.connect()`-open-count for the brief/plan hot paths\" — the canonical description of the gate, in the file this same diff edits, now omits the work-COUNT axis that the CHANGELOG calls the main behavioural change (\"The perf-benchmark suite now counts work, not just connections\"). Cost: the next person adding a benchmark target reads the two-axis description, adds a latency-plus-connection-count pair, and ships exactly the blind spot this PR exists to close.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-3-1",
      "file": "tests/test_perf_benchmarks.py",
      "line": 297,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "The new `_round_floats` work-count bound `counts[\"n\"] <= 100` is not deterministic across dates — the count it measures grows with `date.today()` because `get_training_plan_progress`'s default window is `max(frontier, today) + 7d`, so the assertion goes permanently red on 2026-09-22, 17 days after this head commit, with no code change.",
      "short_summary": "round_floats count bound goes red on 2026-09-22",
      "failure_scenario": "Executed in this checkout at 776ccbd, with `tools.date.today()` faked and everything else untouched: the `_round_floats` call count for `get_training_plan_progress.handler({})` against the module-scoped `perf_db` (built with `today=date(2026,7,9)`) is 25 on 2026-07-09, 79 on 2026-09-01, 83 on 2026-09-05 (real today), 101 on 2026-09-22 — the first date the `<= 100` assertion fails — 109 on 2026-09-30, and it plateaus at 115 from 2026-10-06 onward. The mechanism is cross-file: the handler (src/local_fitness/agent/tools.py:3850-3854) sets `anchor_fwd = max(frontier or today, today)` and `window_end = anchor_fwd + 7d`, so every extra day of wall clock past the fixture's frontier sweeps another prescribed workout dict into `workouts_full`, and each extra dict is one more `_round_floats` recursion. `_ran` is immune (87 at every date probed) only because `build_plan_detail` grades the full plan; the serialized payload is the windowed one. This is the failure class issue #232 item 4 names — a today-relative fixture under a hard-coded expectation — and the test's own header comment at line 285 claims the opposite of it: \"they are deterministic, they do not care which CPU CI drew\".",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "Freeze the clock for the two work-count tests the way the repo already does elsewhere (`tests/test_tools.py:3797` `_freeze_ledger_clock`, `tests/test_mcp_server.py:790`): `monkeypatch.setattr(tools, \"date\", _FakeDate)` pinned to `_TODAY` before calling the handler, then assert the exact count (83 at `_TODAY + 58d`, 25 at `_TODAY`) rather than a `<=` bound.",
      "prior": false
    },
    {
      "id": "c-3-2",
      "file": "tests/test_perf_benchmarks.py",
      "line": 284,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "`assert counts[\"n\"] <= 90` sits 3 calls above the measured 87, and the 87 is a product of the perf fixture's plan length crossed with three separate `_ran` call sites in `plans`, so an unrelated fixture or grading change red-lines it without any regression.",
      "short_summary": "_ran bound of 90 has 3 calls of headroom over 87",
      "failure_scenario": "Measured at head against the same fixture: 87 `_ran` evaluations per request, attributed 36 to `_workout_actuals` and 51 to the generator expressions in `_running_distance`/`_walking_distance`/`_running_duration` (plans.py:189, 201, 213), reached from `classify_workout` (plans.py:339, 351) and `weekly_mileage` (plans.py:543). Each graded day with one on-foot activity therefore costs ~3 evaluations, so adding a single week of prescribed workouts to `scripts/perf_fixture.py`, or turning on `cfg.count_walks_mileage` (which swaps `weekly_mileage`'s `dist_fn` to `_foot_distance` and would change the count), pushes the total past 90 and fails a test whose stated subject is \"did the fold survive\". The docstring's own numbers (123 before, 87 after) show the real signal band is ~36 wide; a bound 3 above the measurement converts that into a tripwire on the fixture instead.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-3-3",
      "file": "src/local_fitness/plans.py",
      "line": 1259,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "The fold removes one of four passes over each day's activity list, but `grade_workout` on the very next line and `weekly_mileage` further down re-walk the same list through `_running_distance`/`_foot_distance`/`_running_duration`, so 51 of the 87 remaining `_ran` evaluations are still second-pass work the diff's docstring says no longer exists.",
      "short_summary": "grade_workout re-walks the list _workout_actuals walked",
      "failure_scenario": "In `build_plan_detail`, line 1256 calls `_workout_actuals(day, cfg)` (which evaluates `_ran` once per on-foot activity) and line 1259 immediately calls `grade_workout(w, day, frontier, cfg)` on the SAME `day` list, which reaches `classify_workout` -> `_running_distance(day_activities, cfg)` (plans.py:339) or `_running_duration` (plans.py:351) — a second `_ran` per activity — and line 1279's `weekly_mileage(...)` walks each date's list a third time via `dist_fn` (plans.py:543). Instrumented at head with a stack-attributed counter: of 87 `_ran` calls per `get_training_plan_progress` request, `_workout_actuals` accounts for 36 and those generator expressions for 51. The diff's new docstring text at plans.py:1206 states \"Anything else this day needs is computed in the loop below, not in a pass of its own\", and the CHANGELOG presents the regression as closed at -6.0%; the larger share of the duplicated `_ran` work is still there, and the new `<= 90` assertion pins it as acceptable. Predates this diff (the helper calls are 2026-07-22 code) but is squarely inside the invariant this change re-asserts.",
      "introduced_by_diff": false,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-3-4",
      "file": "tests/test_perf_benchmarks.py",
      "line": 498,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "`test_the_round_floats_oracle_can_actually_fail` captures the real payload but omits the anti-vacuity assertion its sibling at line 455 has, so if the handler ever returns an error or `{\"active\": false}` envelope the two-sidedness proof silently narrows to the hand-written case list.",
      "short_summary": "oracle-can-fail test never checks the payload is real",
      "failure_scenario": "`_capture_progress_payload` (line 431) grabs whatever object reaches `_round_floats` first. If the perf fixture stops seeding an active plan, `get_training_plan_progress` returns at tools.py:3765 with `_text({\"active\": False})`, and if it raises into an error envelope it returns `_err(...)`; either way `captured[\"payload\"]` is a two-key dict. `test_round_floats_output_is_identical_to_the_pre_inline_implementation` catches that at line 455 (`assert payload.get(\"workouts\")`); line 498's caller does not, so the mutant check keeps passing on `_ROUND_FLOATS_CASES` alone while the claim it makes — that the oracle can fail over the REAL payload — has quietly stopped being tested. The docstring at line 434 also states \"The first `_round_floats` call of the handler IS the whole payload\", which is only true on the success path.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    payload = _capture_progress_payload(monkeypatch)\n    assert payload.get(\"workouts\"), \"the fixture must produce a real plan payload\"",
      "prior": false
    },
    {
      "id": "c-3-5",
      "file": "tests/test_perf_benchmarks.py",
      "line": 512,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "`test_the_latency_gate_is_still_armed` asserts a raw substring of `.github/workflows/ci.yml`, so a purely cosmetic reformat of the pytest command in CI reds the test while the gate it guards is untouched.",
      "short_summary": "gate test greps ci.yml for a literal command substring",
      "failure_scenario": "The assertion needs the exact bytes `--benchmark-compare-fail=min:15%` to appear in the file. Today they do — ci.yml:72 is `--benchmark-compare=0001 --benchmark-compare-fail=min:15% --no-cov`. Wrapping that flag onto its own YAML continuation line, quoting it, or moving the threshold into a workflow `env:`/matrix variable (`--benchmark-compare-fail=min:${{ env.PERF_THRESHOLD }}%`) leaves the gate exactly as armed but fails this test, and there is no message on the assert to say why. Symmetrically, moving `--benchmark-skip` out of `pyproject.toml`'s `addopts` into a conftest hook or a CI flag passes nothing and fails the second assert for the same reason.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-3-6",
      "file": "tests/test_perf_benchmarks.py",
      "line": 83,
      "side": "RIGHT",
      "category": "reuse",
      "summary": "The diff adds a generic `_count_calls(monkeypatch, module, name)` beside the pre-existing `_count_connect_opens`, which is now literally the same instrument specialised to `db.connect` — two counter helpers in one file where one would do.",
      "short_summary": "_count_connect_opens duplicates the new generic _count_calls",
      "failure_scenario": "Cost, not a crash: `_count_connect_opens` (line 83) and `_count_calls` (line 246) are byte-for-byte the same closure-and-monkeypatch shape, differing only in the hard-coded `db`/`connect` target — `_count_connect_opens(monkeypatch)` is exactly `_count_calls(monkeypatch, db, \"connect\")`. The new helper's own docstring says so (\"`_count_connect_opens`'s instrument pointed at a hot function\"). Anyone hardening the instrument later — resetting on error, asserting the patch was actually installed, recording arguments — now has to find and change both, and the six existing connection-count tests keep using the copy that did not get the fix.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "def _count_connect_opens(monkeypatch):\n    return _count_calls(monkeypatch, db, \"connect\")",
      "prior": false
    },
    {
      "id": "c-3-7",
      "file": "src/local_fitness/agent/tools.py",
      "line": 197,
      "side": "RIGHT",
      "category": "reuse",
      "summary": "The \"which dp does this key get\" ternary is now written three times in one function (lines 190, 197-198, 205); the fast path and the fallback ladder can drift apart on any future precision change, and the vendored oracle only catches that after the fact.",
      "short_summary": "dp-selection ternary duplicated three times in _round_floats",
      "failure_scenario": "Cost, not a crash: `_TEXT_HIGH_DP if <k> in _TEXT_HIGH_PRECISION_KEYS else _TEXT_DEFAULT_DP` appears at line 190 (scalar entry), 197-198 (dict fast path) and 205 (list fast path). A change to the precision policy — a third tier, a per-key dp map, or a prefix rule such as \"any key ending `_pace_sec_per_km`\" — has to be applied at all three sites; miss one and the fast path silently rounds differently from the `isinstance` fallback for the same key, which is exactly the divergence `test_round_floats_output_is_identical_to_the_pre_inline_implementation` exists to detect, i.e. it is caught in CI rather than prevented. A module-level lookup keeps it to one place without reintroducing the per-node Python call the change removed.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "Hoist the policy to a module constant next to `_TEXT_HIGH_PRECISION_KEYS` — `_TEXT_DP_BY_KEY = {k: _TEXT_HIGH_DP for k in _TEXT_HIGH_PRECISION_KEYS}` — and write each of the three sites as `_TEXT_DP_BY_KEY.get(k, _TEXT_DEFAULT_DP)` (a dict `.get` on a frozenset-sized map, no extra Python-level call).",
      "prior": false
    },
    {
      "id": "c-3-8",
      "file": "src/local_fitness/plans.py",
      "line": 339,
      "side": "RIGHT",
      "category": "reuse",
      "summary": "`classify_workout` re-derives the running/foot distance for a day that `_workout_actuals` has already computed and returned in the same `build_plan_detail` iteration — the folded five-tuple is the existing helper the grading branch should be reading.",
      "short_summary": "classify_workout re-derives what _workout_actuals returns",
      "failure_scenario": "Cost, not a crash: `build_plan_detail` line 1256 already holds `(actual_dist, run_m, walk_m, actual_pace, actual_types)` for `day` when line 1259 calls `grade_workout(w, day, frontier, cfg)`, whose `classify_workout` recomputes `_foot_distance(day_activities)`/`_running_distance(day_activities, cfg)` (line 337-340) and `_running_duration(day_activities, cfg)` (line 351) from the raw list. `_foot_distance` is `actual_dist`, `_running_distance` is `run_m`, and `_walking_distance` is `walk_m` by construction — three sums re-derived from data already in hand, and the duplicated `_ran` calls behind them are 51 of the 87 measured per request. It also duplicates the pace-gate policy in two places, so the run/walk definition can drift between the surfaced actuals and the verdict — the very disagreement PR #162's classifier fix was written to close.",
      "introduced_by_diff": false,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-3-9",
      "file": "tests/test_perf_benchmarks.py",
      "line": 287,
      "side": "RIGHT",
      "category": "reuse",
      "summary": "The new work-count tests hand-roll their handler invocation instead of reusing the repo's existing clock-freezing helper, which is the one thing that would make a count taken over a today-relative fixture reproducible.",
      "short_summary": "new count tests skip the repo's existing clock-freeze helper",
      "failure_scenario": "Cost, plus the concrete red in the `<= 100` candidate above: `tests/test_tools.py:3797` already defines `_freeze_ledger_clock(monkeypatch, day)` and `tests/test_mcp_server.py:790` uses the same `monkeypatch.setattr(<module>, \"date\", _FakeDate)` shape, so the repo has a settled pattern for pinning `date.today()` in a handler test. Neither new test at lines 279 and 287 uses it, so both measure a fixture that is anchored to `_TODAY = 2026-07-09` through a handler that is anchored to the wall clock — and the counts they assert are only reproducible on the day they were taken. A third copy of the pattern (rather than a shared `_freeze_clock` in `tests/`) is the cheap version of the same duplication.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-3-10",
      "file": "tests/test_plans.py",
      "line": 582,
      "side": "RIGHT",
      "category": "reuse",
      "summary": "Four renamed tests now reach the class list by positional index `[4]` on the five-tuple, spreading knowledge of the tuple's shape across four more call sites where a named accessor (or a `NamedTuple` return) would keep it in one.",
      "short_summary": "four tests index _workout_actuals's five-tuple by [4]",
      "failure_scenario": "Cost, not a crash: lines 582, 583, 602 and 609 each read `plans._workout_actuals(...)[4]`, and `tests/test_perf_benchmarks.py:376` compares the whole tuple positionally too. `_workout_actuals`'s contract is now `(foot_m, run_m, walk_m, pace, activity_types)` with the ordinal written down in src (plans.py:1184 docstring), in `build_plan_detail`'s unpack (plans.py:1256), and in six test expressions. Adding a sixth element is safe, but reordering or inserting one silently changes what four tests assert about — and the deleted `_normalize_activity_types` was the named entry point those four tests used to call, so this is a readability regression the fold created rather than pre-existing.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "Add a one-line accessor in the test module — `def _types(activities, cfg=plans._DEFAULT_GRADING_CONFIG): return plans._workout_actuals(activities, cfg)[4]` — and call it from the four assertions, or have `_workout_actuals` return a `NamedTuple` so `.activity_types` reads at every site.",
      "prior": false
    },
    {
      "id": "c-4-1",
      "file": "tests/test_perf_benchmarks.py",
      "line": 284,
      "side": "RIGHT",
      "category": "intent",
      "summary": "The handler-level `_ran` count assertion is an upper bound with no floor and no payload check, so it passes over zero work whenever the handler returns early.",
      "short_summary": "_ran<=90 passes vacuously when no plan is active",
      "failure_scenario": "`get_training_plan_progress.handler` returns `_text({\"active\": False})` at tools.py:3765 whenever `plans.get_active_plan` is None. Measured in the worktree: with the perf fixture's plan row flipped to status='completed', `plans._ran` is called 0 times and the assertion `counts[\"n\"] <= 90` passes green. Proof 2 in the plan is the one that says the fold actually reached the handler ('proof 1 alone can be satisfied by a folded helper nobody calls'), and it stops saying that the moment `_seed_plan` drifts, `get_active_plan`'s status filter changes, or the fixture's plan ages out. The sibling oracle test at :455 already carries the guard this one lacks (`assert payload.get(\"workouts\")`), and this same diff writes an explicit anti-vacuity floor at :373 with the comment 'A query that quietly matched nothing must fail here, not pass over an empty loop' — so the change applies the rule to one of its three new count assertions and not to the other two.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    assert 60 <= counts[\"n\"] <= 90",
      "prior": false
    },
    {
      "id": "c-4-2",
      "file": "tests/test_perf_benchmarks.py",
      "line": 297,
      "side": "RIGHT",
      "category": "intent",
      "summary": "The `_round_floats` count assertion has the same vacuity hole — an early-return handler produces exactly one call, which is comfortably under the bound.",
      "short_summary": "_round_floats<=100 passes vacuously on an empty payload",
      "failure_scenario": "Measured in the worktree with no active plan: `_text({\"active\": False})` drives `tools._round_floats` exactly 1 time, and `assert counts[\"n\"] <= 100` passes. Its own docstring calls it 'the assertion that binds the `_round_floats` change to a real reduction in work', which is exactly the claim that evaporates: a 1-call run and an 83-call run are indistinguishable to it. Anything that shrinks the fixture payload — a plan that stops seeding, `build_plan_detail` projecting fewer fields, a future compaction pass — silently converts this from a work-count proof into a tautology, and unlike a latency reading nothing else in CI would notice.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    assert 60 <= counts[\"n\"] <= 100",
      "prior": false
    },
    {
      "id": "c-4-3",
      "file": "src/local_fitness/agent/tools.py",
      "line": 169,
      "side": "RIGHT",
      "category": "intent",
      "summary": "The new docstring says the `bool` guard 'becomes load-bearing' under the int fast path, but the fast paths dispatch on exact `type()`, so the guard is still completely inert.",
      "short_summary": "docstring wrongly calls the bool guard load-bearing",
      "failure_scenario": "The plan predicted the guard would stop being dead code because the chosen implementation 'does' write an int fast path. It writes an exact-type one: `elif t is str or t is int or t is bool or v is None` (:198, :210). `type(True) is int` is False, so a bool never reaches the int arm — it is copied by the explicit `t is bool` arm — and the fallback ladder still has no int branch. Verified in the worktree by running the head implementation against a byte-identical copy with `if isinstance(obj, bool): return obj` deleted: 30,000 randomly generated payloads (dict/list/float/int subclasses, IntEnum, tuples, NaN/inf, -0.0, bytes, sets) plus targeted probes `True`, `False`, `{\"x\": True}`, `[True, False]`, `(True,)`, `{\"aerobic_te\": True}`, `[[True]]`, `{\"a\": [True]}` — zero divergences by both `repr` and `json.dumps`. The concrete cost is a reader (or a later refactor) trusting a stated invariant that no code and no test enforces: whoever next simplifies `_round_floats` will read this sentence, believe the guard is protected, and it is not. The plan's own rule — 'no test may claim removing it is observable' — is honoured; the docstring is what breaks it.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    below would make it load-bearing only if that path used ``isinstance``;",
      "prior": false
    },
    {
      "id": "c-4-4",
      "file": "tests/test_perf_benchmarks.py",
      "line": 373,
      "side": "RIGHT",
      "category": "intent",
      "summary": "The `_workout_actuals` equivalence oracle's '548 activity-days' corpus is 548 copies of a single shape, so its anti-vacuity floor counts days rather than distinct behaviour.",
      "short_summary": "548-day oracle corpus is 548 copies of one case",
      "failure_scenario": "Measured against `build_perf_fixture_db` in the worktree: 548 dates, every one holding exactly 1 activity, all 548 typed `running`, and `avg_pace_sec_per_km` NULL on all 548 (scripts/perf_fixture.py:50-60 makes the paceless-ness deliberate). Every single day therefore returns `('running',)` via `_ran`'s label fallback — one branch, 548 times. The fold this oracle guards rewired three things the corpus never reaches: the walking branch, the `classes.add('other')`-then-`continue` for a non-foot row now sitting inside the accumulation loop, and any day holding more than one activity. `_WORKOUT_ACTUALS_EDGE_CASES` (:347) does not close it either — all six entries are single-element lists, so no case in this file mixes an on-foot and a non-foot activity in one day, which is precisely the interaction the fold moved. Concretely: a fold that dropped `classes.add('other')` on a mixed day would leave this test green over all 548 days and all 6 edge cases. (tests/test_plans.py:553 does cover the mixed day, so the repo is not blind — but the oracle sold in the diff and the plan as the guard for the fold is not the thing catching it, and `assert len(by_date) > 500` reads as if it were.)",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-5",
      "file": "tests/test_perf_benchmarks.py",
      "line": 400,
      "side": "RIGHT",
      "category": "intent",
      "summary": "The adversarial case list covers a `dict` subclass (and mutates against it) but has no `list` subclass, leaving the symmetric `type(obj) is list` exclusion unguarded.",
      "short_summary": "no list-subclass case for the symmetric type() fast path",
      "failure_scenario": "The new code has two exact-type fast paths, `type(obj) is dict` (:192) and `type(obj) is list` (:204), and both deliberately shunt subclasses to the `isinstance` ladder. The corpus carries `_DictSubclass` (:419) and `test_the_round_floats_oracle_can_actually_fail` even builds a dedicated `_mishandles_a_dict_subclass` mutant for it — but nothing exercises a `list` subclass. A future edit that widened `type(obj) is list` to `isinstance(obj, list)`, or narrowed the ladder's `isinstance(obj, (list, tuple))`, would change how a list subclass round-trips and both proof 5 and proof 6 would stay green: proof 6 asserts only that *some* case diverges for each mutant, so the missing case is invisible there too. The same gap covers a tuple held as a dict value (`{\"k\": (1.2,)}`) — the dict fast path's `else` arm with a non-container child — which no case reaches either.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-6",
      "file": "tests/test_perf_benchmarks.py",
      "line": 347,
      "side": "RIGHT",
      "category": "intent",
      "summary": "`_WORKOUT_ACTUALS_EDGE_CASES` has no length floor, unlike its sibling `_ROUND_FLOATS_CASES`, so the only discriminating half of the fold oracle can be trimmed silently.",
      "short_summary": "edge-case list has no min-length floor",
      "failure_scenario": "`test_round_floats_output_is_identical_to_the_pre_inline_implementation` pins its corpus with `assert len(_ROUND_FLOATS_CASES) >= 20` (:458). `test_workout_actuals_still_answers_exactly_what_it_answered_before` pins the fixture side with `assert len(by_date) > 500` (:373) but nothing on the edge-case side — and per the finding above, the edge cases are the only part of that oracle that exercises anything other than a paceless labelled run. Deleting entries from the list (a merge resolution, a 'flaky case' removal) shrinks the discriminating corpus to zero while the test still reports a 548-day pass.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    assert len(_WORKOUT_ACTUALS_EDGE_CASES) >= 6",
      "prior": false
    },
    {
      "id": "c-4-7",
      "file": "src/local_fitness/agent/tools.py",
      "line": 204,
      "side": "RIGHT",
      "category": "simplification",
      "summary": "The `dict` and `list` fast paths are 12 lines of near-identical copy-paste differing only in how the result is accumulated and where `dp` is resolved.",
      "short_summary": "dict and list fast paths are copy-paste twins",
      "failure_scenario": "Cost: the scalar-dispatch rule (`float` -> round, `str`/`int`/`bool`/`None` -> copy, anything else -> recurse) is now written twice, at :192-203 and :204-215, and any future change to it — a `decimal.Decimal` arm, a new high-precision key rule, a `bytes` case — has to be applied to both or the two containers silently disagree. The equivalence oracle would catch a divergence only if the corpus happens to hold that type in both a dict and a list position, which today it does not (see the list-subclass finding). The simpler form is one scalar helper the two loops share, e.g. `def _scalar(v, dp): return round(v, dp) if type(v) is float else v` plus a single `_SCALARS = (str, int, bool, type(None))` membership test, leaving each container branch as a three-line loop.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-8",
      "file": "tests/test_perf_benchmarks.py",
      "line": 246,
      "side": "RIGHT",
      "category": "simplification",
      "summary": "`_count_calls` is a strict generalisation of the existing `_count_connect_opens`, and the diff adds it without collapsing the older one into it.",
      "short_summary": "_count_calls supersedes _count_connect_opens; both kept",
      "failure_scenario": "Cost: two counting instruments in one file with identical bodies. `_count_connect_opens(monkeypatch)` (:83) is exactly `_count_calls(monkeypatch, db, \"connect\")`; the new helper's own docstring says so ('`_count_connect_opens`'s instrument pointed at a hot function'). The four existing connection tests could call the general one and the special-cased helper be deleted, so a later fix to the counter (thread-safety, resetting between calls, counting kwargs) does not have to be made twice or, worse, get made once and leave the two axes measuring subtly different things.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-9",
      "file": "tests/test_perf_benchmarks.py",
      "line": 474,
      "side": "RIGHT",
      "category": "simplification",
      "summary": "The two mutants in the oracle-can-fail test are full 8-line re-implementations of `_round_floats_reference` rather than deltas from it.",
      "short_summary": "two mutants re-type the whole reference ladder",
      "failure_scenario": "Cost: the same five-branch ladder is now written three times in the file — `_round_floats_reference` (:333), `_ignores_high_precision_keys` (:474) and `_mishandles_a_dict_subclass` (:485) — with a one-line difference each. When the reference is refreshed (a new branch, a new high-precision rule), the two mutants have to be re-derived by hand or they stop being 'the reference minus one thing' and start being two unrelated functions that happen to diverge, which is not what the test claims to prove. A single `_mutant(kind)` closure, or passing the one differing behaviour in, keeps the delta visible and one-line.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-10",
      "file": "tests/test_perf_benchmarks.py",
      "line": 440,
      "side": "RIGHT",
      "category": "simplification",
      "summary": "`_capture_progress_payload`'s wrapper is installed on the module attribute, so `captured.setdefault` runs on every recursive call, and the helper re-runs the whole handler once per test.",
      "short_summary": "capture wrapper fires on all 83 recursive calls",
      "failure_scenario": "Cost: the docstring says 'The first `_round_floats` call of the handler IS the whole payload', which is true — but because the real implementation recurses through the patched module global, `capturing` is entered 83 times per handler call and does a redundant `setdefault` 82 of them. It also means the helper is silently coupled to the recursion strategy it is measuring: if `_round_floats` ever stopped recursing through the module attribute, the capture would still work but the two work-count tests above would start reading 1. Two tests each pay a full `get_training_plan_progress` run for a payload that could be captured once by a module-scoped fixture and shared. Simpler: capture with an explicit `if \"payload\" not in captured` and hoist the whole thing to a fixture.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    }
  ]
}
