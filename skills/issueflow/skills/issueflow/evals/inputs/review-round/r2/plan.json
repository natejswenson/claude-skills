{
  "verifiers": 8,
  "candidateIds": [
    "c-1-1",
    "c-1-2",
    "c-1-3",
    "c-1-4",
    "c-2-1",
    "c-3-2",
    "c-1-7",
    "c-1-8",
    "c-4-8",
    "c-1-10",
    "c-2-2",
    "c-2-3",
    "c-2-4",
    "c-2-5",
    "c-4-9",
    "c-2-7",
    "c-2-8",
    "c-4-5",
    "c-3-5",
    "c-3-6",
    "c-3-7",
    "c-3-8",
    "c-4-2",
    "c-4-4",
    "c-4-6",
    "c-4-7",
    "c-4-10"
  ],
  "priorIds": [
    "f-9674e7de",
    "f-eea114d4",
    "f-0583160f",
    "f-7098c483",
    "f-7115b02e",
    "f-73a7ebc6",
    "f-0dd0d0da",
    "f-63b3df6d",
    "f-6c527dc9",
    "f-7ede0eb0",
    "f-6dbf8b0e",
    "f-9124fbce",
    "f-7704a32a",
    "f-b9986c2f",
    "f-5f91ea2a",
    "f-4006dff0",
    "f-3d9ed162",
    "f-91fa75b9",
    "f-de08442c",
    "f-283d2ef7",
    "f-7304a058",
    "f-b608107a",
    "f-bbf94366",
    "f-8e1b825a",
    "f-64004f72",
    "f-87a32893",
    "f-a06265fc",
    "f-c0797478",
    "f-474265e6",
    "f-68bf670c",
    "f-8e190b4d",
    "f-0e170358",
    "f-d44090f3",
    "f-87bad23f",
    "f-708c84ed"
  ],
  "candidates": [
    {
      "id": "c-1-1",
      "file": "tests/test_perf_benchmarks.py",
      "line": 116,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "The benchmarked handler reads the real wall clock while its fixture pins today=_TODAY, so this benchmark's workload grows with the calendar — a second, unattributed source of the +13.86% that this change does not close and that keeps creeping until 2026-10-17.",
      "short_summary": "progress benchmark's workload grows with wall-clock date",
      "failure_scenario": "perf_db is built with today=date(2026,7,9); test_bench_get_training_plan_progress calls tools.get_training_plan_progress.handler({}), which windows on date.today() (tools.py:3765, :3852-3854 anchor_fwd = max(frontier, today) + 7d). Measured in this checkout against the fixture, on the POST-FIX code: at _TODAY+17d (2026-07-26, the day the committed 0001 baseline was captured on 8edfb70) the serialized payload is 5,447 chars / 11 workouts; at _TODAY+55d (2026-09-02, the CI run the investigation quotes at +13.86%) it is 9,946 chars / 39 workouts; at _TODAY+100d (2026-10-17) it plateaus at 13,869 chars / 56 workouts. Interleaved in-process A/B of the handler across those three frozen dates, 3 rounds x 40 iterations, min: 897.5us / 926.1us / 943.7us — +3.18% at the CI date and +5.15% at plateau versus the baseline-capture date, on identical code. Consequence: the gate reading is part code and part calendar; the fix removes ~6pp of code cost but the remaining drift keeps rising for another six weeks, so 'lands first so the three notes PRs stop inheriting a validate job that is one runner draw from red' is not what this change buys. The diff's own _freeze_progress_clock (tests/test_perf_benchmarks.py:287) documents exactly this mechanism for the count axis and then leaves the latency axis unfrozen.",
      "introduced_by_diff": false,
      "same_as": null,
      "suggestion": "def test_bench_get_training_plan_progress(benchmark, default_db, monkeypatch):\n    _freeze_progress_clock(monkeypatch, _TODAY)",
      "prior": false
    },
    {
      "id": "c-1-2",
      "file": "CLAUDE.md",
      "line": 200,
      "side": "RIGHT",
      "category": "conventions",
      "summary": "The new drift-vs-regression rule (3) asserts that one benchmark out of line with its siblings is necessarily a code regression, but three of the five benchmarks pin `today=_TODAY` and two read the real clock, so the two can diverge from the three with no code change at all.",
      "short_summary": "New CLAUDE.md rule (3) is false for this benchmark suite",
      "failure_scenario": "CLAUDE.md:200-202 now states: 'One benchmark out of line with its four siblings is a code regression however slow the runner was.' In tests/test_perf_benchmarks.py, test_bench_assemble_brief_context (:107), test_bench_build_plan_section (:128) and test_bench_daily_snapshot (:133) all pass `_TODAY.isoformat()` explicitly, while test_bench_get_training_plan_progress (:116) and test_bench_get_training_plan_status (:121) call `handler({})` and window on `date.today()`. The three pinned targets therefore do a fixed amount of work forever; the two unpinned ones do more work every day. Measured above: the progress payload alone grew 5,447 -> 9,946 chars between the baseline-capture date and the CI run quoted as evidence, worth +3.18% on identical code. So the exact shape the rule cites as proof of a code regression (assemble_brief_context -0.34% beside get_training_plan_progress +13.86%) is also the shape calendar drift produces here, and the new rule tells the next reader to stop looking. It is being written into CLAUDE.md as a durable rule, which is where a wrong one costs the most.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-1-3",
      "file": "src/local_fitness/plans.py",
      "line": 1205,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "The new docstring sentence 'Anything else this day needs is computed in the loop below, not in a pass of its own' is false of the function's only caller: build_plan_detail re-walks the same activities through weekly_mileage, which accounts for 36 of the 87 remaining _ran evaluations.",
      "short_summary": "Docstring says one pass; weekly_mileage re-walks the day",
      "failure_scenario": "build_plan_detail (plans.py:1256) calls _workout_actuals(day, cfg) per workout, then at plans.py:1273 calls weekly_mileage(plan['workouts'], activities_by_date, cfg), which for every week re-walks each date's activity list through _running_distance (plans.py:188) or _foot_distance — _running_distance calls _ran once per activity all over again. Instrumented in this checkout with plans._ran counted and weekly_mileage wrapped, one get_training_plan_progress.handler({}) request at the frozen _TODAY+58d clock: 87 total _ran evaluations, of which 36 (41%) happen inside weekly_mileage over lists _workout_actuals has already walked. The docstring now tells a future maintainer the duplicate-pass class of defect has been eliminated for this data, which is the same false confidence the pre-#162 version of this docstring created and which the paragraph directly above it says 'has now been broken twice'.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "mergedFrom": [
        "c-3-3"
      ],
      "mergedLines": [
        1205
      ],
      "prior": false
    },
    {
      "id": "c-1-4",
      "file": "tests/test_perf_benchmarks.py",
      "line": 570,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "test_the_latency_gate_is_still_armed never asserts that the perf-gate step still passes --benchmark-only, so the single most likely gate demotion — dropping that flag — leaves the test green while the gate silently measures nothing.",
      "short_summary": "Gate guard misses the --benchmark-only demotion",
      "failure_scenario": "The test checks only that '--benchmark-compare-fail=min:15%' appears somewhere in .github/workflows/ci.yml and that '--benchmark-skip' appears in pyproject's addopts. pyproject.toml:59 puts --benchmark-skip in the global addopts, so if someone removes `--benchmark-only` from ci.yml's 'Perf-benchmark regression gate' step (.github/workflows/ci.yml:70), addopts skips all six benchmark tests, --benchmark-compare=0001 --benchmark-compare-fail=min:15% compares an empty set, and the step exits 0 forever. Both of this test's assertions still pass. The docstring says 'The failure mode this change could quietly turn into is a gate demotion' — this is the cheapest gate demotion available and the test cannot see it. Same hole for --benchmark-compare=0001 and for the committed .benchmarks/Linux-CPython-3.12-64bit/0001_*.json file existing at all.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    ci = (root / \".github/workflows/ci.yml\").read_text()\n    assert \"--benchmark-only\" in ci\n    assert \"--benchmark-compare=0001\" in ci\n    assert \"--benchmark-compare-fail=min:15%\" in ci\n    assert list(root.glob(\".benchmarks/Linux-CPython-3.12-64bit/0001_*.json\"))",
      "prior": false
    },
    {
      "id": "c-2-1",
      "file": "tests/test_perf_benchmarks.py",
      "line": 478,
      "side": "RIGHT",
      "category": "language-pitfalls",
      "summary": "`_capture_progress_payload` never freezes the clock, so both equivalence-oracle tests run over a payload whose size and contents depend on the wall-clock date and the machine's local timezone — while the two call-count tests 180 lines above pin the same handler to `_TODAY` + 58d.",
      "short_summary": "oracle payload is captured on an unfrozen wall clock",
      "failure_scenario": "`get_training_plan_progress` windows `workouts` to `[frontier-14d, max(frontier, today)+7d]` and reads `today` from `date.today()` (local timezone). `_capture_progress_payload` calls the handler with no `_freeze_progress_clock`, so on 2026-09-05 the captured payload carries ~83 nodes and on 2027-01-01 it carries the plateau shape (~115 per the sibling docstring), and a CI runner in UTC captures a different payload from a dev machine in America/Chicago on the same evening. Two consequences: (a) the '83 calls' claim and the 'byte-identical output' claim are made about DIFFERENT payloads, so the oracle does not certify the payload the count test measures; (b) a divergence the oracle catches on one date is not reproducible on another, and a case shape that drops out of the window (e.g. a graded day carrying `actual_pace_sec_per_km`) silently stops being covered while the test still passes.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    _freeze_progress_clock(monkeypatch, _TODAY + timedelta(days=58))",
      "mergedFrom": [
        "c-1-5",
        "c-4-1"
      ],
      "mergedLines": [
        478
      ],
      "prior": false
    },
    {
      "id": "c-3-2",
      "file": "tests/test_perf_benchmarks.py",
      "line": 330,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "The `_round_floats == 83` pin is a function of the perf fixture's plan span and of the payload carrying one list per workout row, not of the rounding change, so an edit to `scripts/perf_fixture.py` or a payload compaction in `tools.py` breaks it.",
      "short_summary": "83 is 3 + 2*windowed-workouts, set by the fixture",
      "failure_scenario": "The count decomposes exactly as `3 + 2*W`: one call for the top-level payload dict, one for `this_week`, one for the `workouts` list, then one per workout dict and one per that row's `actual_activity_types` list. W is the windowed workout count, which comes from `scripts/perf_fixture.py:137` (`range(-70, 98, 2)`) intersected with the handler's `[frontier-14d, today+7d]` window — 11 at `_TODAY` (25 calls), 40 at `_TODAY`+58d (83), 56 from `_TODAY`+100d (115), which is precisely the table in `_freeze_progress_clock`'s docstring. Two consequences: (a) `scripts/perf_fixture.py` is the SHARED fixture that five other benchmarks read, so changing `_RUN_EVERY_N_DAYS` or the plan's day range for an unrelated benchmark silently moves this number and the failure names the `_round_floats` fix; (b) half of the 83 is the per-row `actual_activity_types` list, so extending `get_training_plan_progress`'s existing 'omit None-valued keys at build time' compaction (tools.py:3809-3824) to also omit an EMPTY list — a strictly better payload — drops the count to ~43 and reds a test whose stated purpose is to prove work went down.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "mergedFrom": [
        "c-1-6"
      ],
      "mergedLines": [
        330
      ],
      "prior": false
    },
    {
      "id": "c-1-7",
      "file": "src/local_fitness/agent/tools.py",
      "line": 178,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "The docstring's '470 ... for a single 10 KB get_training_plan_progress payload' / 'the same payload costs 83' are true only for a real clock inside a two-day window, but are written as properties of the payload — repeated in CHANGELOG.md:28 and CLAUDE.md:207 with no caveat.",
      "short_summary": "470/83 stated as payload facts; true only at _TODAY+57..58d",
      "failure_scenario": "Measured in this checkout against the perf fixture: pre-inline node count / post-inline call count is 157 / 25 at _TODAY, 461 / 81 at +56d, 470 / 83 at +57d and +58d, and 644 / 115 from +100d. Only the count test freezes the clock; the docstring, the CHANGELOG entry and the CLAUDE.md worked example do not say so. A maintainer who re-measures on any other day (e.g. 2026-11-01, where it is 644 -> 115) gets a different pair, cannot reconcile it with three documents that all say 470 -> 83, and reasonably concludes the payload or the function has drifted since. The '10 KB' figure has the same problem: the payload is 5.4 KB at the baseline-capture date and 13.9 KB at plateau.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "mergedFrom": [
        "c-4-3"
      ],
      "mergedLines": [
        178
      ],
      "prior": false
    },
    {
      "id": "c-1-8",
      "file": "tests/test_perf_benchmarks.py",
      "line": 401,
      "side": "RIGHT",
      "category": "line-by-line",
      "summary": "The _workout_actuals equivalence oracle never runs a non-default GradingConfig and its 548-day corpus is entirely paceless, so the fold's `cfg` threading through the relocated `_ran` call is untested by the proof that exists to guard the fold.",
      "short_summary": "Fold oracle never exercises a non-default GradingConfig",
      "failure_scenario": "scripts/perf_fixture.py:197-207 inserts every activity as activity_type='running' with no avg_pace_sec_per_km column, so _ran's pace gate (plans.py:150-155) never fires on any of the 548 days and cfg.pace_gated_locomotion is never actually consulted. Every entry in _WORKOUT_ACTUALS_EDGE_CASES is likewise called through `plans._workout_actuals(case)` with no cfg. If the fold had written `if _ran(a):` instead of `if _ran(a, cfg):` when moving the call, this oracle — the proof whose stated job is 'the fold removed a pass, not a classification' — would pass unchanged on all 555 inputs. It survives only because tests/test_plans.py:609 happens to cover it in a different file.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    off = plans.GradingConfig(pace_gated_locomotion=False)\n    for case in _WORKOUT_ACTUALS_EDGE_CASES:\n        assert plans._workout_actuals(case, off) == _workout_actuals_reference(case, off), case",
      "prior": false
    },
    {
      "id": "c-4-8",
      "file": "tests/test_perf_benchmarks.py",
      "line": 312,
      "side": "RIGHT",
      "category": "intent",
      "summary": "The plan's Proof 2 and Proof 3 specify upper bounds (`_ran` <= 90, `_round_floats` <= 100); the head asserts exact equality (87 and 83 at :330) with no Deviations entry recording the change.",
      "short_summary": "Plan asked for <=90/<=100; diff pins ==87/==83",
      "failure_scenario": "The plan wrote both as bounds precisely so the assertion tracks the invariant ('the doubled work is gone') rather than a snapshot. Pinning exactly makes any unrelated change to the progress payload's shape — one more prescribed-workout field, one more rollup dict — turn `ci / validate`'s coverage step red on a perf test with a number the author of that change cannot re-derive (83 is not a fixture property; it is the node count on 2026-09-05, see the clock finding). The implement artifact's Deviations section still describes the shipped behaviour as '`_ran` <= 90 and `_round_floats` <= 100', so the record of the change disagrees with the change. I note this responds to f-0583160f / f-7098c483 (the `<=` form passing vacuously) — the verifier should weigh whether an anti-vacuity floor plus a bound (`87 <= n <= 90`) satisfies both concerns rather than choosing one.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    assert 87 <= counts[\"n\"] <= 90",
      "mergedFrom": [
        "c-1-9",
        "c-3-1"
      ],
      "mergedLines": [
        312
      ],
      "prior": false
    },
    {
      "id": "c-1-10",
      "file": "CLAUDE.md",
      "line": 230,
      "side": "RIGHT",
      "category": "conventions",
      "summary": "PR #236 is opened from a feature branch straight into `main`, which the repo's own branch rule forbids.",
      "short_summary": "Feature branch PR'd into main, not dev",
      "failure_scenario": "CLAUDE.md:230-233 states: '**Work through `feature/* -> dev -> main`.** Normal changes land via a PR into `dev`, then a `dev -> main` promotion — never a direct push to `main`/`dev` (admin break-glass aside).' CLAUDE.md:128-131 reinforces it: 'The live deployment tracks `dev`, not `main` ... Do **not** promote to `main` or cut a release as part of normal work — that happens only when Nate explicitly asks.' PR #236's base is `main` with head `feature/issue-232-perf-plan-progress-regression`. Merging it puts the change on the public-consumption snapshot without it ever landing on `dev`, so the live container (built from `dev`) never gets the fix and `dev` and `main` diverge. Cited on the rule line because the violation is the PR's base ref, not a diff line.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-2-2",
      "file": "tests/test_perf_benchmarks.py",
      "line": 301,
      "side": "RIGHT",
      "category": "language-pitfalls",
      "summary": "The clock freeze patches exactly one module attribute (`tools.date`) and nothing asserts that is the only clock the handler reads, yet two exact-equality count pins (87 and 83) depend on the freeze being total.",
      "short_summary": "freeze patches only tools.date; no check it is total",
      "failure_scenario": "`monkeypatch.setattr(tools, 'date', _FakeDate)` rebinds the name `date` in `local_fitness.agent.tools` only. It holds today because `plans` has no `date.today()` call, but `get_training_plan_progress` reaches `plans.build_plan_detail`, `plans.best_recent_effort`, `config.riegel_lookback_days` and `units.display_units()`, none of which is frozen. Move the window computation into `plans` (or have `units`/`config` read `date.today()`), and the pinned counts silently become wall-clock-dependent again: the test then goes red on a calendar schedule and reads as a perf regression rather than a leaked clock. Nothing in the test asserts the frozen date is the one the handler actually used — e.g. no assertion on `window_end` or on the number of workouts in the payload.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-2-3",
      "file": "tests/test_perf_benchmarks.py",
      "line": 465,
      "side": "RIGHT",
      "category": "language-pitfalls",
      "summary": "`_dumped`'s `json.dumps(..., default=str)` normalizes every subclass case in `_ROUND_FLOATS_CASES` (`_IntSubclass`, `_Grade`, `_DictSubclass`, `_FloatSubclass`) to its base JSON form, so the four cases added specifically to justify the `type(x) is ...` fast paths can only detect a value divergence, never the type divergence they exist to guard.",
      "short_summary": "json oracle is blind to the subclass cases' whole point",
      "failure_scenario": "`json.dumps(_IntSubclass(7)) == json.dumps(7)`, `json.dumps(_Grade.OK) == json.dumps(2)`, `json.dumps(_DictSubclass({'a':1})) == json.dumps({'a':1})`, `json.dumps(_FloatSubclass(1.23)) == json.dumps(1.23)`. So a fast-path change that converts a dict-subclass value to a plain dict, or an IntEnum to a plain int, passes `test_round_floats_output_is_identical_to_the_pre_inline_implementation` unchanged. The diff already recognised this blindness for tuple-vs-list and bolted on explicit `isinstance` asserts at the end of that test, but did not extend the same treatment to the four subclass cases — which are the only reason those cases are in the list at all.",
      "introduced_by_diff": true,
      "same_as": "f-7ede0eb0",
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-2-4",
      "file": "src/local_fitness/agent/tools.py",
      "line": 199,
      "side": "RIGHT",
      "category": "removed-behaviour",
      "summary": "The single `isinstance` dispatch was replaced by a hand-maintained literal type list in two places (`t is str or t is int or t is bool or v is None`), and nothing structurally ties that list to the `isinstance` ladder it must stay identical to — the only guard is 21 remembered cases in a test.",
      "short_summary": "fast-path scalar type list can drift from the ladder",
      "failure_scenario": "The docstring states 'the fast path must stay semantically identical to the fallback'. That property is now maintained by two duplicated hand-written type lists (lines 199 and 211) against a five-branch ladder 15 lines below. Concretely: someone adds `or t is tuple` to the scalar list as an obvious pass-through. The ladder at line 218 still converts tuples to lists, so `_round_floats({'splits': (1.0, 2.0)})` returns a tuple while `_round_floats([(1.0, 2.0)])`'s inner value returns a list — inconsistent output from one function. `_dumped`'s json comparison cannot see it (f-7ede0eb0), and the explicit tuple asserts only cover a bare tuple and a list-of-tuples, not a tuple inside a dict, so the equivalence oracle passes.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-2-5",
      "file": "tests/test_perf_benchmarks.py",
      "line": 423,
      "side": "RIGHT",
      "category": "language-pitfalls",
      "summary": "The `_workout_actuals` oracle compares two accumulated floats and a float division with `==` (exact float equality) rather than `pytest.approx`, which the file's sibling tests in `test_plans.py` use for exactly these fields.",
      "short_summary": "oracle compares accumulated floats with ==, not approx",
      "failure_scenario": "`foot`, `run`, `walk` are `+=` accumulations and `pace = dur / (foot / 1000.0)`. The assertion only holds bit-for-bit because the fold happens to accumulate in the same order as the vendored reference. Any future refactor that sums run and walk separately and derives `foot = run + walk`, or reorders the loop, changes the last bit and reddens this test over a change that moved no answer — the exact failure mode the oracle is supposed to distinguish itself from. It is also NaN-unsafe: a row whose `duration_seconds` is `float('nan')` makes `pace` NaN on both sides and the tuple `==` then evaluates False, so the oracle reports a divergence between two implementations that agree. `tests/test_plans.py:558-560` already uses `pytest.approx` for the same three values.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-9",
      "file": "tests/test_plans.py",
      "line": 566,
      "side": "RIGHT",
      "category": "intent",
      "summary": "The retargeted comment now attributes the pre-2026-07-22 label classification to `_workout_actuals`, which never contained it — the label classification lived in `_normalize_activity_types`, the function this diff deletes.",
      "short_summary": "Comment credits _workout_actuals with history it never had",
      "failure_scenario": "The comment reads 'Before the pace-gating fix this read [\"other\", \"running\"] — `_workout_actuals` classified it by the \"treadmill_running\" LABEL alone'. Before the pace-gating fix `_workout_actuals` returned a four-tuple and did no classification at all; the label-based classifier was `_normalize_activity_types`, whose own (now-deleted) docstring recorded exactly that history. A reader running `git log -S` on `_workout_actuals` to find when it classified by label finds nothing, and the one record of the real history is the deleted function. The plan asked for the comment to 'name `_workout_actuals`' — it can do that without rewriting who did what.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    # read [\"other\", \"running\"] — the classification (then a separate helper,\n    # `_normalize_activity_types`, now folded into the loop below) went by the",
      "mergedFrom": [
        "c-2-6"
      ],
      "mergedLines": [
        566
      ],
      "prior": false
    },
    {
      "id": "c-2-7",
      "file": "tests/test_perf_benchmarks.py",
      "line": 309,
      "side": "RIGHT",
      "category": "language-pitfalls",
      "summary": "The frozen date `_TODAY + timedelta(days=58)` is unexplained and equals the wall-clock date the tests were written (2026-09-05), so the two exact counts are pinned at one arbitrary point on a curve the helper's own docstring documents as varying 25 → 83 → 115.",
      "short_summary": "58d freeze is the authoring date, not a chosen regime",
      "failure_scenario": "`_freeze_progress_clock`'s docstring states the `_round_floats` count is 25 at `_TODAY`, 83 at `_TODAY` + 58d, and plateaus at 115 from `_TODAY` + 100d. `_TODAY` is 2026-07-09 and +58d is 2026-09-05, the date the diff was authored — i.e. the freeze reproduces the unfrozen behaviour of that one day rather than a deliberately chosen operating point. The payload it measures is dominated by 58 days of ungraded `pending` future rows (a two-month-stale data frontier), not by the graded history the regression was found in; at a realistic frontier (`_TODAY`) the count is 25, so the 470 → 83 headline is quoted against a shape the handler rarely serves. Nothing in the test says why 58 was chosen, so a future maintainer who re-freezes at a different date cannot tell whether the new number is a regression or a different point on the same curve.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-2-8",
      "file": "src/local_fitness/plans.py",
      "line": 1238,
      "side": "RIGHT",
      "category": "removed-behaviour",
      "summary": "Deleting `_normalize_activity_types` removes the module's only standalone activity classifier while its three single-purpose siblings that walk the same list (`_running_distance`, `_walking_distance`, `_foot_distance`) are all kept, so the class list is now reachable only by running the full five-value accumulation and discarding four results.",
      "short_summary": "only the classifier lost its standalone helper form",
      "failure_scenario": "`_running_distance` (:181), `_walking_distance` (:193) and `_foot_distance` (:217) all survive as independently callable helpers even though `_workout_actuals` also computes each of them in its single pass — so the fold's argument ('anything else this day needs is computed in the loop below, not in a pass of its own') is applied to exactly one of the four. The concrete cost is already visible in the diff: four tests in `tests/test_plans.py` (:582, :583, :602, :609) now call `plans._workout_actuals(...)[4]`, a positional index into a five-tuple, to assert a classification, and any future caller wanting only the class list must do the same.",
      "introduced_by_diff": true,
      "same_as": "f-8e1b825a",
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-5",
      "file": "tests/test_perf_benchmarks.py",
      "line": 566,
      "side": "RIGHT",
      "category": "intent",
      "summary": "`test_the_latency_gate_is_still_armed` claims to guard three things and asserts two — the plan named three demotion routes (no threshold change, no `continue-on-error`, no rebaseline) and only the threshold and the skip default are checked.",
      "short_summary": "Gate guard names the baseline but never asserts it",
      "failure_scenario": "The docstring says the fix 'does not touch the threshold, the baseline, or the `--benchmark-skip` default'. The body greps ci.yml for `--benchmark-compare-fail=min:15%` and reads `--benchmark-skip` out of pyproject's addopts. Nothing asserts (a) `--benchmark-compare=0001` is still on the perf step, (b) `.benchmarks/Linux-CPython-3.12-64bit/0001_*.json` is still the committed baseline, or (c) the `Perf-benchmark regression gate` step has no `continue-on-error`. A later change that adds `continue-on-error: true` to that step, or replaces the `0001_*.json` capture with one taken on the regressed code, demotes the gate exactly as the plan warned and this test — the one written to stop that — stays green. Related to but distinct from f-283d2ef7 and f-9674e7de, which are about the form and vacuity of the greps that do exist.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "mergedFrom": [
        "c-3-4"
      ],
      "mergedLines": [
        566
      ],
      "prior": false
    },
    {
      "id": "c-3-5",
      "file": "tests/test_perf_benchmarks.py",
      "line": 426,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "The `_workout_actuals` equivalence oracle never passes a non-default `GradingConfig`, so a fold that dropped `cfg` from the in-loop classification would satisfy it over all 548 fixture days and all 7 edge cases.",
      "short_summary": "Oracle never exercises a non-default GradingConfig",
      "failure_scenario": "Both loops call the live and the reference implementation with the default config (`plans._workout_actuals(activities)` / `(case)` at lines 423-426), and `_workout_actuals_reference`'s own `cfg=plans._DEFAULT_GRADING_CONFIG` parameter is therefore dead. The fold moved the classification into a loop that must thread `cfg` into `_ran(a, cfg)` (plans.py:1231); write `_ran(a)` there instead and every assertion in this test still passes, because the default config is what both sides use. The only thing that catches it is `tests/test_plans.py:609` (`test_activity_types_pace_gate_can_be_turned_off`), a different file that is not part of the byte-for-byte equivalence proof this test is presented as. Add a `pace_gated_locomotion=False` pass over the same corpus, or at least one edge case run under a non-default cfg.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-3-6",
      "file": "tests/test_perf_benchmarks.py",
      "line": 335,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "The comment calls the vendored references 'deliberately frozen copies, not imports', but all three read live symbols out of `plans` and `tools`, so a change to those symbols moves both sides of the oracle together.",
      "short_summary": "The \"frozen\" reference copies call live src symbols",
      "failure_scenario": "`_normalize_activity_types_reference` (line 338) and `_workout_actuals_reference` (line 350) call `plans._is_on_foot` and `plans._ran`, and default their `cfg` to the live `plans._DEFAULT_GRADING_CONFIG`; `_round_floats_reference` (line 366) reads the live `tools._TEXT_HIGH_DP`, `tools._TEXT_DEFAULT_DP` and `tools._TEXT_HIGH_PRECISION_KEYS`. Concretely: add a key to `_TEXT_HIGH_PRECISION_KEYS` (tools.py:146-149) and get the fast path's inline lookup wrong — say the dict branch keeps using the child key `k` where the intended semantics for the new key is the enclosing key — and the reference, reading the same frozenset through the same variable, cannot show the divergence for any case whose shape the fixed `_ROUND_FLOATS_CASES` list does not already contain. Same for a change to `_ran`'s pace gate: both implementations move, the oracle stays green, and the comment tells the next reader the reference still 'describes the OLD behaviour'. Freeze the constants the references depend on (copy the dp values and the key set into the test module) if the copies are meant to be frozen.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-3-7",
      "file": "tests/test_perf_benchmarks.py",
      "line": 501,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "The two equivalence-oracle tests run the handler on an unfrozen clock while the two count tests freeze it at `_TODAY`+58d, so 'the real payload' the oracle proves over is not the payload the counts pin.",
      "short_summary": "Oracle payload and counted payload are different payloads",
      "failure_scenario": "`test_get_training_plan_progress_does_not_double_classify` (line 304) and `..._rounds_without_a_call_per_node` (line 315) both call `_freeze_progress_clock(monkeypatch, _TODAY + timedelta(days=58))` before capturing counts; `test_round_floats_output_is_identical_to_the_pre_inline_implementation` (line 501) and `test_the_round_floats_oracle_can_actually_fail` (line 604's capture) call `_capture_progress_payload` with no freeze at all, so the payload they compare is whatever the real wall-clock date produces (`window_end = today + 7d`, tools.py:3853). Today that happens to be the same window; on any other date it is not. The consequence is that the two halves of the proof — 'output is identical over the real payload' and 'the payload costs 83 calls' — are statements about different objects, and a fast-path bug that only shows up for the rows outside the unfrozen window would be counted but never compared. Freeze both, using the same helper, so the proof and the count describe one payload.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-3-8",
      "file": "src/local_fitness/plans.py",
      "line": 193,
      "side": "RIGHT",
      "category": "cross-file",
      "summary": "`_walking_distance` has no caller anywhere in `src/` — it is kept alive only by its own unit tests, which is the exact rule this diff used to delete `_normalize_activity_types`.",
      "short_summary": "_walking_distance has no src caller, left in place",
      "failure_scenario": "Grep over `src/` finds `_walking_distance` only at its own definition (plans.py:193) and in `_running_distance`'s docstring at line 196; the only executable references are `tests/test_plans.py:463, 511, 542`. The run/walk split that production actually serves comes from `_workout_actuals`'s single pass (`walk += d`, plans.py:1234). The diff removed `_normalize_activity_types` on the stated grounds that 'the fold left it with no caller' (CHANGELOG.md:21) but left its sibling in the same helper block under the same condition, so the module still advertises four one-pass distance helpers of which one is unreachable. Cost: a reader looking for how walk distance is computed finds two implementations and no way to tell which one ships, and the dead one is separately maintained by three tests.",
      "introduced_by_diff": false,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-2",
      "file": "tests/test_perf_benchmarks.py",
      "line": 287,
      "side": "RIGHT",
      "category": "intent",
      "summary": "`_freeze_progress_clock` is a mechanism the approved plan never named and the implement stage's Deviations section does not record, and it turns 83 into a property of one chosen calendar day rather than of the perf fixture.",
      "short_summary": "Clock freeze is an undeclared deviation from the plan",
      "failure_scenario": "The plan's Files table and Proofs 1-8 describe counters around `plans._ran` and `tools._round_floats` and say nothing about the wall clock; the Deviations section of the implement artifact lists three carry-out notes and states 'None from the plan's substance', none of them the clock. Measured in the checkout, the choice matters: the handler's window is `max(frontier, today) + 7d`, so the same fixture yields 25, 55, 83, 85 or 115 `_round_floats` calls depending on the date supplied. +58d was chosen because it is the authoring day, not because anything about the fixture points at it, so a maintainer reading `assert counts[\"n\"] == 83` cannot re-derive either number from the fixture and has no principle for updating it (the plateau at +100d, 115, would have been re-derivable).",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-4",
      "file": "tests/test_perf_benchmarks.py",
      "line": 447,
      "side": "RIGHT",
      "category": "intent",
      "summary": "The `list` fast path's scalar clause (`t is str or t is int or t is bool or v is None`) is not exercised by the differential corpus at all — no case in `_ROUND_FLOATS_CASES`, and nothing in the real payload, puts a non-float scalar inside a list.",
      "short_summary": "No case puts a bool/int/str/None inside a list",
      "failure_scenario": "Mutation-verified in the checkout. I built a mutant of the shipped `_round_floats` differing only in the list branch — a `bool` element rounded (`out_l.append(round(v, dp))`) instead of copied, so `[True]` serializes as `[1]` — and ran the exact assertion `test_round_floats_output_is_identical_to_the_pre_inline_implementation` makes, over the captured handler payload plus all 21 `_ROUND_FLOATS_CASES`: **0 divergences**. The proof-5 corpus therefore cannot see a defect in the clause the diff added. Controls: the same harness caught `list_else_passthrough` (2 divergences) and `dict_else_passthrough` (3), so the corpus does reach the container-recursion branches — it is specifically the list scalar clause that is unreached. The cases containing lists are `[(...,), (...,)]` (tuples), `{\"aerobic_te\": [floats]}`, `{\"day\": {...}}` and `{\"rows\": [{...}]}`; the only list elements anywhere are floats, tuples and dicts.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": "    {\"rows\": [{\"avg_pace_sec_per_km\": 333.2222672948015, \"ok\": True,\n               \"name\": \"Run\", \"reps\": 4, \"note\": None,\n               \"splits\": [1.111111111, 2.222222222],\n               \"flags\": [True, False, None, 4, \"easy\", 1.23456789]}]},",
      "prior": false
    },
    {
      "id": "c-4-6",
      "file": "CLAUDE.md",
      "line": 176,
      "side": "RIGHT",
      "category": "intent",
      "summary": "CLAUDE.md still says the perf-benchmark suite is 'Skipped on every ordinary `pytest` run' and that the validate job 'opts back in' — the diff deliberately makes seven tests in that file the exact inverse, and this bullet was edited in the same change without being corrected.",
      "short_summary": "CLAUDE.md still says the whole perf file is skipped",
      "failure_scenario": "The bullet at :159 is scoped to `tests/test_perf_benchmarks.py` as a whole. Verified in the checkout: `uv run pytest tests/test_perf_benchmarks.py` reports `16 passed, 6 skipped` — the seven new work-count/oracle/gate tests run on an ordinary pytest run, and are the ones skipped under the validate job's `--benchmark-only`. A maintainer following CLAUDE.md concludes a red work-count assertion surfaces in the 'Perf-benchmark regression gate' step, when the diff's own module docstring (:26-28) says it surfaces in 'Run tests with coverage gate'. The diff added 23 lines to this very section (part (3) of the tell) and left the contradicting sentence 17 lines above it. Same class as f-6c527dc9 / f-3d9ed162 but a different claim on a different line.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-7",
      "file": "pyproject.toml",
      "line": 56,
      "side": "RIGHT",
      "category": "intent",
      "summary": "The `addopts` comment says 'the perf-benchmark suite (tests/test_perf_benchmarks.py) only runs when explicitly requested via `--benchmark-only`', which the diff makes false for seven tests in that file — a second place the same contract is written and was not updated.",
      "short_summary": "pyproject comment: whole perf file needs --benchmark-only",
      "failure_scenario": "`test_the_latency_gate_is_still_armed` reads this very file's `addopts` as its guard, so the diff already treats this file as part of the contract, yet the sentence three lines above the string it reads is now wrong. Verified: those seven tests run under a bare `uv run pytest`. A contributor trusting this comment adds a slow, network-touching or fixture-heavy assertion to `tests/test_perf_benchmarks.py` believing it is opt-in, and it lands in the default coverage job on every PR.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    },
    {
      "id": "c-4-10",
      "file": "tests/test_plans.py",
      "line": 577,
      "side": "RIGHT",
      "category": "intent",
      "summary": "`test_activity_types_classify_by_pace_not_label` still calls itself a 'Direct unit test of the classifier itself' after the retarget, when there is no longer any way to reach the classifier directly and the test now drives the whole accumulation loop.",
      "short_summary": "Docstring still claims a direct unit test of the classifier",
      "failure_scenario": "The classifier is now three lines inside `_workout_actuals`'s loop, reached only through distance/duration accumulation and pace computation. The test's second paragraph already says so ('there is no separate helper to call'), directly contradicting its own first line. A reader diagnosing a classification bug trusts the label 'direct unit test', concludes the classifier is isolated, and is surprised when a change to distance accumulation reds it.",
      "introduced_by_diff": true,
      "same_as": null,
      "suggestion": null,
      "prior": false
    }
  ]
}
