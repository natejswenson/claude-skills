# X — x.com/natejswenson (found via the handle sweep)

The profile cannot be read logged-out: the page returns HTTP 402 to anonymous
fetches, the syndication endpoint returns 200 with an empty body, and no
search result surfaces the handle. Existence and ownership are corroborated
out of band: the subject's authenticated Typefully workspace lists exactly one
social set — username "NatejSwenson", display name "Nate Swenson".

No post content could be snapshotted; only the account's existence and
ownership are recorded here.
