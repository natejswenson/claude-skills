# X — x.com/natejswenson (@NatejSwenson)

Now discoverable from the anchor itself: the GitHub profile's
`twitter_username` field names NatejSwenson, so a stranger who finds the
GitHub account finds the X account in one hop. The profile still cannot be
read logged-out (HTTP 402 to anonymous fetches; the syndication endpoint
answers 200 with an empty body), so no post content is snapshotted — only
existence and ownership are recorded. Ownership is additionally corroborated
by the subject's authenticated Typefully workspace, whose single social set
is username "NatejSwenson", display name "Nate Swenson".
