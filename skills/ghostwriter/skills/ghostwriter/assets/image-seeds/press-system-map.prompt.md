# Seed receipt: system-map

Created from the `twenty-plugins-one-implementation` post as a native Codex image-generation
run. Use the PNG as a **style reference only**.

- Information shape: one shared system branching to two consumers through an explicit adapter.
- PRESS anatomy: top rule, NS stamp, small masthead, giant stacked headline, serif standfirst,
  labeled system map, ruled colophon.
- Palette source: `~/.claude/ghostwriter/assets/diagram.css`, `.card.press`.
- Variation fingerprint: centered stacked headline · branching hero · no marginal numeral ·
  medium density · letterpress grain.
- Proven text-fidelity pattern: enumerate every permitted string under `Text (verbatim)` and
  explicitly ban extra, duplicate, clipped, or misspelled copy.

Do not reuse its subject, wording, diagram, or centered layout. A future card must change at
least two variation axes recorded in `images/generated-card-history.jsonl`.
