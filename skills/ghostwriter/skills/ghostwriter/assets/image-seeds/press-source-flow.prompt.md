# Seed receipt: source-flow

Created by passing `press-system-map.png` as a style-only reference, then requesting a new
information hierarchy for the same post. Use the PNG as a **style reference only**.

- Information shape: source metadata flows through a generator while implementation files form
  one shared foundation.
- PRESS anatomy: top rule, NS stamp, small masthead, left display headline, orange marginal
  numeral, horizontal flow, shared foundation, serif marginal note, bottom rule.
- Palette source: `~/.claude/ghostwriter/assets/diagram.css`, `.card.press`.
- Variation fingerprint: left headline · marginal numeral · horizontal flow · dense technical
  hero · letterpress grain.
- Forward-test result: brand, all requested text, and the alternate layout survived a native
  reference-image generation in one pass.

Do not reuse its subject, wording, diagram, or left-headline layout. A future card must change at
least two variation axes recorded in `images/generated-card-history.jsonl`.
