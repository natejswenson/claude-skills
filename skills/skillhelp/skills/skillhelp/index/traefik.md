# traefik

<!-- GENERATED by @natjswenson/skillhelp — do not edit. Run `skillhelp build`. -->

- **version** — 0.1.0
- **stack** — node
- **summary** — Manage a local Traefik Docker stack through its separately installed tk CLI. Use for "check Traefik health", "diagnose local HTTPS or DNS", "show service logs", "preview a rebuild", and "remove a service". Supports Codex and Claude Code with explicit stack resolution and scoped operations. `skills/traefik/skills/traefik/SKILL.md:3`
- **triggers** — "check Traefik health", "diagnose local HTTPS or DNS", "preview a rebuild", "remove a service", "show service logs"

## Setup

- **Claude Code:** Local file/shell tools, Node 18+, and an external stack with executable scripts/tk. No app connection is needed for stack operations. `skills/traefik/README.md:77`
- **Codex:** The same runtimes and CLI, with normal sandbox approval for Docker or host networking where required. Claude app/MCP connections are not imported. `skills/traefik/README.md:79`
- **Personal data:** Both hosts share ${XDG_CONFIG_HOME:-~/.config}/traefik-skill/settings.json, containing only a schema version and the selected absolute stack path. The file is mode 0600. Compose fi… `skills/traefik/README.md:81`
- The external [traefik-local-cli](https://github.com/natejswenson/traefik-local-cli) must support tk capabilities --json with agent protocol 1 and the capabilities listed in [operations.md](skills/tra… `skills/traefik/README.md:86`
- See [Codex migration notes](../../docs/codex-migration.md) for host tool handling. `skills/traefik/README.md:92`
- Requires Node >=18 (package.json engines). `skills/traefik/skills/traefik/package.json:33`
- Reads environment variable PATH. `skills/traefik/skills/traefik/scripts/traefik.js:64`
- Reads environment variable TK_TEST_MODE. `skills/traefik/skills/traefik/scripts/tests/helper.test.mjs:21`
- Reads environment variable TRAEFIK_DIR. `skills/traefik/skills/traefik/scripts/traefik.js:47`
- Reads environment variable XDG_CONFIG_HOME. `skills/traefik/skills/traefik/scripts/traefik.js:17`

## Usage

- Triggers on: Manage a local Traefik Docker stack through its separately installed tk CLI. Use for "check Traefik health", "diagnose local HTTPS or DNS", "show service logs", "preview a rebuild", and… `skills/traefik/skills/traefik/SKILL.md:3`
- Keep the operating instructions with your other skills while the CLI, Compose configuration and application data stay in their own repositories. Both hosts use one skill and one local stack binding.… `skills/traefik/README.md:15`
- Path — What it provides `skills/traefik/README.md:22`
- skills/traefik/SKILL.md — Shared Codex and Claude operating instructions. `skills/traefik/README.md:24`
- skills/traefik/scripts/traefik.js — Stack discovery, local binding and CLI compatibility checks. `skills/traefik/README.md:25`
- skills/traefik/references/operations.md — Command selection, failure handling and ownership boundaries. `skills/traefik/README.md:26`
- skills/traefik/evals/ — A real CLI contract, replay output and an incompatible-contract trap. `skills/traefik/README.md:27`
- Claude Code — run in chat: `skills/traefik/README.md:31`
- Use /traefik:traefik if Claude displays the plugin-qualified command. For a local development checkout, register its absolute path as the marketplace source instead. `skills/traefik/README.md:39`
- Codex — run in a terminal from this repository's root: `skills/traefik/README.md:42`
- Start a new Codex session, then invoke in chat: `skills/traefik/README.md:49`
- With the skill loaded, ask to bind your Traefik checkout as the default. For manual setup, run from the directory containing the loaded SKILL.md: `skills/traefik/README.md:56`

## Commands

- node scripts/traefik.js bind --stack /absolute/path/to/traefik `skills/traefik/README.md:60`
- node scripts/traefik.js detect `skills/traefik/README.md:61`
- npm run audit — npm audit --audit-level=moderate `skills/traefik/skills/traefik/package.json:38`
- npm run postpack — rm -f README.md LICENSE CHANGELOG.md `skills/traefik/skills/traefik/package.json:40`
- npm run prepack — cp ../../README.md ../../LICENSE ../../CHANGELOG.md . `skills/traefik/skills/traefik/package.json:39`
- npm run test — node --test "scripts/**/*.test.mjs" `skills/traefik/skills/traefik/package.json:37`
- Binary "traefik-skill" → scripts/traefik.js `skills/traefik/skills/traefik/package.json:22`

## Architecture

- The shared plugin owns instructions and a small locator/compatibility checker. The external tk repository owns every stack operation. skill-invariants.json records deterministic commands and model ju… `skills/traefik/skills/traefik/SKILL.md:95`
- Deterministic: Resolve and check the selected stack — node scripts/traefik.js detect `skills/traefik/skills/traefik/skill-invariants.json:26`
- Deterministic: Bind a verified local default — node scripts/traefik.js bind `skills/traefik/skills/traefik/skill-invariants.json:30`
- Deterministic: Validate the CLI contract offline — node scripts/traefik.js check `skills/traefik/skills/traefik/skill-invariants.json:34`
- Model judgment: Choose the smallest service operation matching the user request — User intent and application evidence determine the target and whether a mutation is authorized. `skills/traefik/skills/traefik/skill-invariants.json:40`
- Model judgment: Diagnose endpoint failures and verify useful recovery — Container readiness alone cannot establish DNS, TLS, authorization or application correctness. `skills/traefik/skills/traefik/skill-invariants.json:44`

## Troubleshooting

- Never operate a stack inferred from the plugin cache; resolv — The one rule. It is the reason this skill exists rather than a prompt; lose the line and the skill becomes a generic assistant with extr… `skills/traefik/skills/traefik/skill-invariants.json:9`
- Never claim a result you did not observe — Honesty about what was verified is the whole house contract. A skill that reports success it did not witness is worse than one that reports nothing. `skills/traefik/skills/traefik/skill-invariants.json:14`
- never ask about anything in it — Detection establishes the selected checkout and compatibility; asking for the same information adds avoidable setup work. `skills/traefik/skills/traefik/skill-invariants.json:19`
- **Never operate a stack inferred from the plugin cache; resolve an explicit or verified stack and use its external tk CLI.** `skills/traefik/skills/traefik/SKILL.md:15`
- **Never claim a result you did not observe.** State the selected stack, affected `skills/traefik/skills/traefik/SKILL.md:85`
